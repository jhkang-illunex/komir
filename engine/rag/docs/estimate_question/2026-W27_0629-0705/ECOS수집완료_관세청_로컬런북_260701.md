---
source: documents/산출물/2026-W27_0629-0705/ECOS수집완료_관세청_로컬런북_260701.md
week: 2026-W27
title: ECOS 수집 완료 · 관세청 로컬 실행 런북 (2026-07-01)
---

# 예상 질문

## 환경 제약 배경
- Cowork 리눅스 셸에서 정부 API(ECOS·공공데이터포털·관세청)가 막히는 이유는 무엇인가?
- ECOS는 어떻게 실수집에 성공했는가(우회 경로)?
- 관세청 API는 왜 web_fetch로 수집하지 않고 로컬 파이프라인 실행으로 미뤘는가?

## ECOS 수집 결과(수치)
- raw_ecos에 적재된 series는 몇 개이며 각각의 주기·기간·행수는?
- KR_industrial_production, KR_gdp_real의 ECOS 코드는 각각 무엇인가?
- ECOS 수집 정본 산출물(parquet) 경로는 어디인가?
- ECOS_SERIES 설정과 collect_ecos() 함수는 어떻게 교정되었는가?

## 관세청 월간수입 수집(로컬 실행)
- 관세청 API의 엔드포인트와 수집 대상(HS10 코드 수·기간)은 무엇인가?
- REE=Nd 확정 이후 HS 매핑에서 제외된 코드와 최종 HS 코드 수는 몇 개인가?
- 관세청 API는 1콜당 최대 몇 개월을 조회할 수 있으며, 이를 위해 코드가 어떻게 자동 분할하는가?
- 관세청 수집 결과 검증을 위한 체크리스트 항목은 무엇인가(행수, HS 커버리지, 광종 결합 등)?
- cnt=1000 상한 문제는 어떻게 해결해야 하는가?
- 키 미인증(returnReasonCode=30) 등 문제는 어떤 명령으로 진단하는가?

## 다음 우선순위
- 이 문서 작성 시점에서 가장 시급한 작업(🔴)은 무엇인가?
- 월간 타깃 데이터가 확보되면 예측모델을 어떤 방식(SARIMAX/LightGBM walk-forward)으로 전환할 계획인가?
