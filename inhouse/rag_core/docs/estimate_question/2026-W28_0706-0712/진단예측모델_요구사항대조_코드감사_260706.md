---
source: documents/산출물/2026-W28_0706-0712/진단예측모델_요구사항대조_코드감사_260706.md
week: 2026-W28
title: 수급위기 진단모델(①)·수입 예측모델(②) — 코드 근거 상세 감사 (2026-07-06)
---

# 예상 질문

## 감사 범위 / 방법
- 이 감사가 다루는 산출물과 제외한 산출물은 각각 무엇인가?
- 감사 방법(소스코드 직접 판독)에서 배제한 것은 무엇인가?
- 과업지시서가 요구한 진단모델·예측모델의 핵심 요건은 각각 무엇인가?

## komir 진단모델 — 피처 실측
- `diagnosis.py`의 `FEATS` 목록에 들어 있는 9개 피처는 무엇인가?
- 계약 필수변수 ①②③⑤는 각각 어떤 컬럼으로 매핑되며 실측 상태는?
- ④세계공급부족이 "코드 자체가 없다"고 판정된 근거는 무엇인가(`marts.py:110`)?
- `supply_shortage` 컬럼이 만들어지는데도 모델에 연결되지 않는 이유는?
- ⑥지정학이 항상 NULL인 이유는 무엇이며, grep으로 무엇을 확인했는가?
- ⑤생산독점도가 "부분"인 이유(신버전 marts.py vs 구버전 weekly_mart.py)는?
- `diagnosis.py`의 자체 방어 로직(`nunique()>=2`)은 어떤 결과를 낳는가?
- 6변수 중 확실히 살아있는 것은 몇 개이며, 나머지가 빠진 이유가 "데이터 부족"이 아닌 이유는?

## komir 진단모델 — 모델 비교·경보
- 코드가 실제로 비교하는 3개 모델은 무엇이며 어떤 지표를 기록하는가?
- 계약이 예시한 알고리즘(로지스틱회귀·DT·RF)과 일치하지 않음에도 "구조적으로 충족"으로 본 이유는?
- 위기 이진분류의 임계값은 얼마인가?
- `alert.py`의 3단 구조(A 분위수·B 규칙 오버라이드·C 히스테리시스)는 각각 무엇인가?
- 규칙 오버라이드의 3가지 트리거는 무엇인가?
- `alert_reason.py`의 `OFFICIAL` 문안은 무엇을 하는가?

## 순환성 / 학습데이터 문제
- `crisis_index`의 정의(`alert.py:34`)는 무엇인가?
- 순환성 문제가 발생하는 구조를 설명하라.
- 현재 웨어하우스의 `teacher_supply_demand`는 무엇으로 채워져 있는가?
- 그로 인해 보고된 AUC 0.977은 어떤 성격의 수치인가?

## Nd·HS바스켓
- komir에 반영된 REE 9코드의 구성은 무엇인가?
- komis의 comtrade collector는 희토류를 어떤 코드로 잡고 있으며 무엇이 빠졌는가?

## komir 예측모델
- `forecast.py`의 `_LAGS`·`_FEATS`·`_TARGETS`는 각각 무엇인가?
- 타깃이 계약과 "문자 그대로 일치"한다는 것은 어떤 의미인가?
- 복합 경제변수 미포함이란 구체적으로 무엇이 빠졌다는 뜻인가?
- 비교모델이 없다는 지적의 근거는 무엇인가?
- "walk-forward"라는 표현이 실제 코드와 다른 이유는?
- R² 0.897/0.866은 어떤 검증 구간 기준으로 해석해야 하는가?
- `fact_trade_monthly`의 데이터 규모와 기간은?
- REE 처리에서 코드가 놓치고 있는 신뢰도 차이는 무엇인가?

## komis 진단모델(SDIS)
- SDIS의 6개 구성요소와 각각의 가중치는 얼마인가?
- 계약 필수변수 중 SDIS에 대응이 없는 것은 무엇인가?
- `supply_hhi`가 ②가 아니라 ⑤에 대응하는 이유는?
- `supply.py`의 데이터 한계(IEA 2025 단일 연도 정적표)는 무엇인가?
- ⑥지정학에 대응하는 SDIS 요소 3개와 합계 가중치는?
- `china_share`는 계약 변수 중 어디에 해당하는가?
- SDIS의 경보 등급 명칭과 법정 명칭의 불일치는 무엇인가?

## komis 예측모델
- `make_label`의 학습 타깃은 무엇인가?
- 계약 요구와 산출 대상·형태가 어떻게 전부 다른가?
- `data/imports.csv`의 수입량 데이터는 어떻게 쓰이고 있는가?
- 코발트 수입량 값에서 발견된 이상 징후는 무엇인가?
- komis에 "수입 예측 모델링"이 없다는 결론의 근거는?

## [정정] 예측모델의 지정학 변수 요건
- 최초 감사에서 무엇을 놓쳤는가?
- 과업지시서 2쪽 설계전략에서 니켈과 희토류에 대해 명시한 문구는 각각 무엇인가?
- komir와 komis가 "정확히 반대 방향으로" 요건을 어긴다는 것은 무슨 뜻인가?
- komis `train_model_prob()`의 피처 목록에는 어떤 지정학 관련 항목이 있는가?
- 보완된 권고에서 komir가 raw GDELT 대신 무엇을 재사용해야 한다고 했는가?

## 종합 매트릭스
- komir와 komis의 ① 6변수 연결 개수는 각각 얼마인가?
- 경보 4단계 법정 명칭 일치 여부는 각각 어떠한가?
- ② 산출 형태·주기·복합변수·지정학 변수·실데이터 항목에서 두 저장소의 판정은?

## 우선순위 제안
- 가장 저비용·고효과라고 지목한 수정은 무엇인가?
- ④세계공급부족 변수 신설의 데이터 출처 후보는?
- 예측모델 백테스트를 어떻게 전환해야 하는가?
- 진단모델 학습데이터에 대한 권고는?
- komis를 어떻게 재배치하라고 권고했는가?

## [2차 재검토] 붙임1·붙임2 숨은 요구사항
- 가장 큰 누락으로 지목된 항목은 무엇인가?
- 과업지시서 Step2 예시에서 동·니켈/리튬·코발트·희토류의 가중치 지침은 각각 무엇인가?
- 두 구현체가 "단일 풀링모델"이라는 판정의 코드 근거는?
- `commodity_code` 더미변수가 학습하는 것과 학습하지 못하는 것은 각각 무엇인가?
- 동적 가중치(Attention·VSN) 부재란 무엇이며 "공간축"과 "시간축" 차등은 어떻게 구분되는가?
- 지표 간 중복성·상관성 분석 코드가 0건이라는 grep 결과는 무엇인가?
- `feature_corr_matrix.csv`가 요건 충족으로 인정되지 않는 이유는?
- 민감도 분석 요건과 `permutation_importance`의 차이는 무엇인가?
- 붙임2의 3계열 병렬 구조란 무엇이며 `alert.py`가 어떻게 우선순위를 역전했는가?
- 그 override조차 실전에서 항상 비활성인 이유는?
- 다중 산출주기 요구(①의 일·주·월, ②의 분기·연간)에 대한 현재 구현 상태는?
- 모델개발보고서 요건 중 코드 감사 대상이 아니지만 짚어둔 항목은?

## [Q&A] 동적 가중치
- "지정학 지수를 입력으로 넣으면 동적 가중치가 해결되는가"에 대한 답은 무엇인가?
- 고정 가중합(SDIS)에서 값이 튀어도 기여 비율이 고정되는 이유는?
- HistGBM/Ridge의 상호작용이 "가중치 상향"으로 인정되기 어려운 이유는?
- `OV_GEO_SEV=0.85` 규칙 오버라이드가 Step3의 어떤 소박한 버전인가?
- Step3 원문이 강제사항이 아니라는 근거는 무엇인가?
- 이 답변으로도 남는 두 가지 문제는 무엇인가?

## [제안] komis 수집기 데이터 흡수
- Google News·mining.com RSS는 어떤 경로로 흡수 가능한가?
- komis의 `event_intensity`/`sent_neg_ratio` 점수를 그대로 가져오면 안 되는 이유는?
- GDELT는 왜 LLM 추출을 거치지 말고 직접 매핑해야 하는가?
- Goldstein scale은 무엇으로 매핑되는가?
- IEA 공급집중표는 왜 ⑥이 아니라 ⑤ 쪽에 붙여야 하는가?
- yfinance·UN Comtrade 이관이 불필요한 이유는?
- 발행처 신뢰도를 등록하지 않으면 어떤 왜곡이 생기는가?
- 중복 이벤트 억제가 필요한 이유("보도량이 심각도로 둔갑")는?
- 이 통합이 해결하지 못하는 두 가지는 무엇인가?

## [실행계획 검토] 1차 지수 → 2차 모델 활용
- 이 순서가 타당하다고 본 근거(메우는 두 구멍)는 무엇인가?
- 지수를 주간·월간 둘 다로 설계해야 하는 이유는?
- 예측모델에서 미래 12개월의 지정학 지수를 채우는 3가지 선택지는?
- `forecast.py`에 외생변수를 넣으려면 무엇을 새로 설계해야 하는가?
- 광종별 차등 가중치를 위해 최소한 어떤 항을 넣을지 결정해야 하는가?
- 백테스트 leakage 방지를 위해 무엇을 지켜야 하는가?
- 일정 관점에서 1차 스코프를 어떻게 좁히라고 권고했는가?

## GKG 벌크 구현·검증
- `gkg_bulk_download.py`의 워커 배정 방식은 어떻게 재설계되었으며 최초 방식의 문제는 무엇이었는가?
- 벌크 저장 위치를 NAS로 결정한 이유(용량 수치)는?
- 실측 처리량과 전체 완료 예상 시간은?
- 구리·니켈에 사용된 World Bank 전용 테마코드는 무엇인가?
- 코발트·리튬·희토류는 왜 키워드 매칭을 쓰는가?
- 발견된 오탐 사례와 추가한 필터는?
- 환경 이슈(권한 문제)는 어떻게 해결했는가?
- 2016년 1개년 파싱 결과 이벤트 총 건수와 광종별 건수·정책성 비율은?
- 문서(보고서)가 실시간 사건 추적에 부적합한 이유는(IEA 발간 주기·SCRREEN 스냅샷 연도)?
- IEA 2025 보고서에서 확인된 광종별 챕터 페이지와 전용 챕터는?
- GKG 표본 검증에서 REE·CO·CU/NI의 오탐 실태는 각각 어떠했는가?
- GKG 오탐의 근본 원인(기사 본문 부재, 테마코드 매칭 방식)은 무엇인가?
- 광종별 종합 판단에서 가장 취약한 광종은 무엇이며 이유는?
- 개선 권고 5가지는 각각 무엇인가?
- 문자 오프셋 근접성 활용이란 무엇을 걸러내는가?
- REE 키워드를 어떻게 좁히라고 권고했는가?
