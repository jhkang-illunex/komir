---
source: documents/산출물/2026-W28_0706-0712/mineral_risk_model_v0.md
week: 2026-W28
title: 광종 수급 리스크 모델 뼈대 v0.1
---

# 예상 질문

## 범위 / 표기 규약
- 이 설계안의 대상 광종 5종은 무엇이며, 희토류는 어떤 원소로 표기되는가?
- 산출물 3종과 각각의 주기·형식은 무엇인가?
- 변수 생산 가능성 범례 `[D]`·`[C]`·`[E]`는 각각 무엇을 뜻하는가?

## 공통 구조 — 상태 변수
- `G[c]`·`T[c,m]`·`F[k]`·`W[m,c]`는 각각 무엇을 나타내는 변수인가?
- `G[c]`의 상태공간 4단계(S0~S3)는 무엇인가?
- `T[c,m]`의 상태 4단계(T0~T3)는 무엇인가?
- 공통 인자 `F[k]`의 4가지 k는 무엇인가?
- 국가 노드 pruning 규칙의 기준(세계 공급 점유율)은?
- 코발트의 host-metal 커플링은 어떤 외생 입력을 추가하는가?
- 네오디뮴의 황산 배선이 중동 채널이 아닌 이유는?
- 황산 순환 커플링에서 들어오는 엣지는 어디서 오는가?

## 이벤트 스키마 / 주간 집계
- 이벤트 스키마의 필드는 무엇이며 `dimension`의 4가지 값은?
- severity는 어떤 척도이며 누가 분류하는가?
- `evt_sev`의 산식에서 severity를 제곱 가중하는 이유는?
- `media_base[c]`는 무엇이며 왜 필요한가?
- `evt_z`는 어떻게 계산되는가?

## ① 지정학 이슈 지수
- 입력 변수 중 벤치마크·보조 공변량으로 쓰이는 공개 데이터는 무엇인가?
- `loss[m,c,s]`의 사례 앵커로 어떤 사건들이 제시되었는가(Grasberg·Kamoa·칠레·페루·DRC)?
- v0의 조합 방식(EWMA halflife, 로지스틱 매핑)은 어떻게 구성되는가?
- v1으로 승격 시 어떤 필터를 쓰며 전이강도 공변량은 무엇인가?
- 시변 Q는 어떤 방식으로 처리되는가?
- `SaR[m,w]`, `tail[m,w]`, `geo_idx[m,w]`의 산식은 각각 무엇인가?
- attribution(`contrib[c]`)은 어떻게 정의되며 무엇에 쓰이는가?

## ② 수급 위기 체계 판단
- ②의 성격이 "글로벌 시장 위기"가 아니라 무엇으로 확정되었으며 그 이유는?
- 6대 축은 각각 무엇이며 변수명·산식·주기는?
- 축2(수입 편중도)에서 12개월 누적 물량 기준을 쓰는 이유는?
- 축3에서 물량 기여와 단가 기여를 분해해 저장하는 이유는?
- 축4(세계 공급부족)의 광종별 소스(ICSG·INSG·Cobalt Institute)와 [D]/[E] 구분은?
- 순환 축과 구조 축은 각각 어떤 번호이며 정규화 방식이 왜 다른가?
- 구조 축에 분위 정규화를 금지한 이유(nd 대중국·co 대DRC)는?
- 구조 축의 절대 임계 매핑 구간(HHI 기준)은 어떻게 되는가?
- 주기 정합에서 as-of join이 필수인 이유와, 관세청·ICSG의 발표 시차는?
- `crisis_score`의 산식과 가중치 초기값은?
- 등급 컷(관심/주의/경계/심각)의 임계값은 각각 얼마인가?
- 구조/순환 분해 리포트는 어떤 서사를 자동 생성하는가?
- 히스테리시스 규칙은 무엇인가?
- v1에서는 어떤 모델을 쓰며 대외 보고 지표는 왜 v0를 유지하는가?

## 보조 패널 / 라벨
- `inv_days`·`spread`·`premium`·`tcrc`가 점수 산식에서 제외된 이유와 대신 어떤 3가지 용도로 쓰이는가?
- v1 학습 라벨을 6축 합성식과 독립으로 만드는 이유는?
- `label_score`의 가중치 구성(price_z·inv_days·spread)은 어떻게 되는가?

## ③ 국내 수입 추정
- 타깃 `import_tons`의 정의와 소스, 교차검증 수단은?
- HS 바스켓 초안에서 cu·ni·co·li·nd의 후보 코드는 각각 무엇인가?
- 8505.11(영구자석)은 왜 별도 관리 대상인가?
- HS 바스켓에 대해 어떤 검수가 필수라고 명시했는가?
- 입력 변수 중 다운스트림 활동 지표 3종은 무엇인가?
- `inv_cycle`은 어떻게 정의되는가?
- v0 모델은 무엇이며 exog로 무엇을 넣는가?
- v1의 팬차트는 어떻게 만들어지며 어떤 분위수를 산출하는가?
- `geo_idx`를 v0에 강제로 넣지 말라고 한 이유(부호 양가성)는?

## 동적 가중치
- "어텐션 메커니즘"을 딥러닝 없이 구현할 수 있다는 근거(Nadaraya-Watson 동치)는?
- 학습형 딥 어텐션이 부적합한 이유(에피소드 표본 규모)는?
- v1.5·v2·v3 각각의 방식과 산식은?
- v2의 상황 벡터 `x_t`는 무엇으로 구성되는가?
- 에피소드 라이브러리 후보 5건은 각각 무엇인가?
- 계수 수축(`β_k ← λ·β_k_local + (1−λ)·β₀`)이 필요한 이유는?
- v2에서 attribution은 어떻게 보고에 활용되는가?
- 검증 방식(leave-one-episode-out)은 무엇인가?

## 검증 훅
- 산출물 ①②③의 검증 지표와 벤치마크는 각각 무엇인가?
- 백테스트 분할 방식과 홀드아웃 에피소드 4건은?
- 컴포넌트별 최적화 방식이 다를 때 "학습 곡선"을 어떻게 다르게 제시해야 하는가?
- LightGBM·베이지안(PyMC)·커널 어텐션·SARIMAX의 보고서 그림은 각각 무엇인가?
- "epoch 곡선을 인위 생성하지 말라"는 지침의 근거 논리는?

## 구현 순서 / 최종 구성
- 제안된 구현 순서 6단계는 무엇인가?
- v0/v1이 폐기되지 않고 v2+에서 어떤 역할로 존속하는가?
- 추론 코어와 딥러닝의 역할 배치가 왜 이렇게 결정되었는가?
