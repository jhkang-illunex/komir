---
source: documents/산출물/2026-W31_0727-0802/시스템_기술서_데이터_전처리_모델링_260727.md
week: 2026-W31
title: 핵심광물 수급위기 진단·수요예측 시스템 — 데이터·전처리·모델링 상세 기술서
---

# 예상 질문

## 문서 성격 / 기준 상태
- 이 기술서의 기준 상태 버전(스코어카드 v1.22 / forecast_unit v4 / aux_early_warning v2)은 무엇을 의미하는가?
- 문서에 실린 수량은 어떻게 확보한 값이며 문서값 재인용이 아니라는 근거는?
- 성능 수치의 정본은 어느 문서인가?
- 시스템은 어떤 3개 모듈로 구성되며 산출은 어디로 발행되는가?

## 정형 데이터 인벤토리(실측)
- 관세청 월간 수출입 데이터의 행수와 커버 개월 수는?
- KOMIS 5광종 기준가격(fact_price)의 행수와 기간은?
- 진단 정답(teacher)으로 쓰는 KOMIS 수급동향은 몇 행이며 어느 시점부터인가?
- LME 재고(CU·NI)와 중국 거래소 재고(SHFE·GFEX)의 실측 행수는 각각 얼마인가?
- UN Comtrade 21흐름은 몇 계열·몇 행이며 중국계 5흐름은 언제 컷 되었는가?
- 파생시장 데이터(SHFE/GFEX 미결제약정, CFTC COT)의 계열 수와 행수는?
- 수요 사이클 계열(OECD CLI·WSTS·ECOS·EIA·Eurostat·akshare)에서 꼬리 정지가 발생한 계열은?
- WoodMac 전망 데이터가 "as-of 주의"로 표시된 이유는?
- fact_series·fact_indicator 합계 행수와 2026-07-25 확충 전 대비 배수는?
- 미수집 잔여 항목은 몇 건이며 어떻게 처리되었는가?

## 비정형 데이터
- geo_event의 실측 건수와 정제 전 건수는 각각 얼마인가?
- 사내 문서 코퍼스는 geo_event에서 어떻게 구분되는가?

## 수집·적재 규약
- 멱등 적재에서 "삭제 스코프"를 어떻게 제한하며 왜 그런 규칙이 생겼는가?
- 빈/부분 응답 가드는 어떤 조건에서 DELETE를 차단하는가?
- cron 스케줄 3종(토 06:30·토 09:10·매월 6일 09:20)은 각각 무엇을 수행하는가?
- 산출물 보존 정책과 색인 문서는 무엇인가?

## 전처리 — 비정형(geo)
- 1차 규칙 파싱에서 CU·NI와 CO·LI·REE의 매칭 방식이 다른 이유는?
- GDELT 전용 테마코드 WB_2934/2935는 어떤 광종에 쓰이는가?
- LLM 재검증에서 추출하는 필드와 Pydantic 검증 게이트의 역할은?
- 관련성 정제 2단계 판정으로 오염률이 어떻게 변했는가(71.4% → 99.5%)?
- geo 파이프라인의 정본 저장소는 DB인가 parquet store인가?
- 2026-07-27 교차판정에서 관찰된 잔존 한계(16/250건, severity 편향)는?

## 전처리 — 정형
- fact_series와 fact_indicator의 스키마 차이는 무엇인가?
- 관세청 데이터의 광종 매핑 방식과 단가 계산식은?
- as-of 조인에서 OECD CLI의 발표 지연은 며칠로 설정되었는가?
- USGS 생산점유 참조자료는 몇 개 릴리스를 as-of로 관리하는가?
- 결측 처리 원칙에서 학습과 예측 시점의 처리가 다른 이유(x_z13 사례)는?
- 표준화·파생 피처(주간 z 52주, 월간 z 24개월, yoy, chg3, log1p, 계절 인코딩)는 각각 어떻게 만드는가?

## 지수화 전처리
- 이벤트 집계 시 row-wise apply를 금지하는 성능 규약은 무엇인가?
- 지수 공식이 tanh 절대스케일인 것의 장점(과거값 불변)과 재앵커 표시 방식은?
- 주간 앵커는 무슨 요일이며 구 스냅샷과의 정합 규약은?

## 모델링 — 공통 검증 원칙
- 모든 채택이 통과해야 하는 3가지 관문은 무엇인가?
- PER_CC_CHAMP는 어떤 함정을 코드화한 것인가?
- 자동 피처선정(순열중요도 등)이 금지된 이유는?

## 모델링 — 급증확률(geo_prob)
- geo_prob의 패널 단위와 타깃(y_next)은 무엇인가?
- base3 피처 3종은 각각 무엇이며 CO에만 추가되는 피처는?
- NB2 회귀에서 burst_k는 어떻게 정하는가?
- k_adapt 기반 적응형 병기가 REE 왜곡을 어떻게 해소했는가(0.52→0.12)?
- LI에 대해 어떤 결론이 확정되었는가?

## 모델링 — 진단(레벨 / Δ 조기경보)
- 레벨 모델 패널의 행수와 정답(teacher) 정의는?
- 레벨 모델 피처 구성(등급 지속성 + 지정학 3종 + 무역 3종 + 가격 4종)은?
- 가격 이격률 등 신규 가격 파생이 기각된 이유는?
- 레벨 모델의 등급 매핑·확률 산출·XAI 사유문 생성은 어떻게 이뤄지는가?
- 레벨 모델 대안이 몇 차례 기각되었는가?
- Δ 조기경보의 타깃 정의와 hard 결합이 금지된 근거는?
- 소프트보팅 두 멤버의 차이(p_burst vs gsev_z13)와 "보팅은 원천이 다를 때만" 규칙의 관계는?
- 양 멤버 공통 피처에 포함된 OECD 한국 CLI 3종은 무엇이며 avail 지연은?
- 광종별 채택 동작점(CU·NI·LI)은 각각 무엇인가?

## 모델링 — 12개월 예측
- 예측 패널의 단위·타깃과 로그 변환 여부는?
- 기본 11피처(BASE)의 구성은 무엇인가?
- MIDAS 피처에서 ton과 unit이 서로 다른 입력을 쓰는 이유는?
- 직접 다지평(direct)을 채택한 근거(재귀 대비 MASE 0.755)는?
- CO ton과 NI unit에 적용된 셀 오버라이드는 각각 무엇인가?
- 구간 추정 방식과 실측 커버리지는?
- 신규 외생 29종 전패가 시사하는 구조(lag 지배)는?

## 재검정 하네스 / 발행 산출물
- 신규 계열을 SERIES_SPEC에 등록하면 어떤 단계가 자동으로 수행되는가?
- "발주처 신규 데이터 제공 시 수일 내 검증 회신 가능"의 근거는 무엇인가?
- 발행 산출물별 테이블·행수(out_diagnosis_alert 1,632행 등)는 어떻게 되는가?
- geo_index·geo_prob의 행수와 갱신 주기는?
- 대시보드 HTML의 크기와 폐쇄망 게시 가능 여부는?
