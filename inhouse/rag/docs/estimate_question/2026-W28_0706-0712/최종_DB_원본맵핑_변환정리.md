---
source: documents/산출물/2026-W28_0706-0712/최종_DB_원본맵핑_변환정리.md
week: 2026-W28
title: 최종 DB화 · 원본→테이블 맵핑 · 변환 내용 정리
---

# 예상 질문

## DB 전체 현황
- 최종 DuckDB는 테이블 몇 개와 뷰 몇 개로 구성되며 원본 문서는 몇 건인가?
- 차원 테이블 4종의 행수는 각각 얼마인가?
- 정량 사실 테이블 6종(fact_trade·fact_price·fact_indicator·fact_series·fact_inventory·fact_production_reserve)의 행수는 각각 얼마인가?
- `doc_raw` 1,092건은 소스별로 어떻게 구성되는가?
- `geo_event` 1,742건에서 baseline과 llm 추출 건수는 각각 얼마인가?
- `fact_geopolitical_weekly` 뷰의 행수와 기준 extractor는?
- 모델 입력 mart 4종의 행수는 각각 얼마인가?

## 원본 → 테이블 맵핑
- KOMIS 공급망통계는 어떤 변환(멀티헤더 파싱)을 거쳐 어느 테이블로 가는가?
- 관세청 수출입DB(43MB xlsx)는 어떤 방식으로 적재되었는가?
- USGS 엑셀정리본은 어떤 변환을 거쳤으며 어떤 컬럼이 제외되었는가?
- BDI·환율·중국 지표 csv의 인코딩은 무엇인가?
- Asian Metal·Argus PDF와 KOMIS PDF는 추출 방식(2단 vs 단단)이 어떻게 다른가?
- 조달청보고서는 몇 건이며 그중 몇 건이 분석되었고 어떤 광종 신호를 얻었는가?
- GSCPI는 현재 어떤 상태인가?
- 적재 상태에서 analyzed 건수와 hold 건수는 각각 얼마인가?
- 미적재·보류로 남은 자료 3종은 무엇인가?

## 변환·처리 방식
- 7개 정량 원천을 통일한 스키마 형태와 공통 키(광종·날짜·단위·출처)는?
- HS 권위 매핑 검증 결과 요약(미매핑·충돌 건수)은?
- 적재 멱등화는 어떤 방식으로 구현했는가?
- 비정형 파이프라인의 2단 분리 구조와 status 기반 흐름은?

## HWP 문제 해결
- 한컴 `hwp_export_pdf`를 쓸 수 없었던 이유는?
- 대신 어떤 기술로 HWP 텍스트를 추출했는가(OLE·zlib·HWPTAG_PARA_TEXT)?
- 구현 파일명은 무엇인가?
- HWP 파싱으로 새로 확보한 역사적 지정학 이벤트 3가지는 무엇인가?
- 같은 파서로 추가 처리 가능한 문서는 몇 건인가?

## 변환 결과 / 모델 준비 상태
- PDF 보고서 1,069건은 어떻게 구성되며 적재 결과는?
- 진단모델과 예측모델의 입력 테이블과 현재 상태는 각각 어떠한가?
- 진단모델에 남아 있는 갭 2가지는 무엇인가?
