# dashboards/streamlit_app.py 이식(model_loaders.py)이 실제로 쓰는 패키지 —
# scikit-learn/shap/duckdb/python-dotenv는 mineral_supply_risk/requirements.txt와
# 동일 최소버전(msr.config의 python-dotenv, forecast_unit.py의 SHAP TreeExplainer,
# db/dbio.py의 duckdb). plotly/streamlit은 API 서빙엔 불필요해 제외.
fastapi
uvicorn[standard]
pydantic-settings
sqlalchemy    # MSR_DB가 서버DB URL로 cutover될 때 db/dbio.py가 필요(§0-1) — 현재 DuckDB 경로엔 불필요
psycopg2-binary
duckdb>=1.1
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
shap>=0.45
python-dotenv>=1.0
