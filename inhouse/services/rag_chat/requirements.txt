# TODO(구현 단계): 실제 버전 고정. rag/ragkit이 이미 쓰는 sentence-transformers/duckdb(→
# 텍스트+메타는 sqlalchemy/psycopg2로 Postgres, 벡터는 qdrant-client로 Qdrant — 2026-08-05
# pgvector안 폐기, docs/CONTAINER_ARCHITECTURE.md §4)에 fastapi/uvicorn(SSE 스트리밍 포함) 추가.
fastapi
uvicorn[standard]
sqlalchemy
psycopg2-binary
qdrant-client
sentence-transformers
sse-starlette
python-docx  # rag/ragkit/ingest.py가 모듈최상단에서 import(2026-08-19 컨테이너 실빌드로
             # 발견 — generate.py→build_index.py→chunk.py→ingest.py 임포트체인에 걸림,
             # 소스트리 실행에선 다른 곳에서 먼저 깔려 있어 안 드러났던 누락)
duckdb>=1.1    # mineral_supply_risk/db/dbio.py(services/shared/db.py가 재노출)
pandas>=2.0    # 〃
matplotlib>=3.7  # rag/ragkit/chatbot_events.py — 인용된 표에서 즉석 차트(PNG) 렌더링,
                 # 2026-08-13 rag 패키지 chatbot 엔트리포인트 신설(다중매체 이벤트)
koreanize-matplotlib>=0.1.1  # 〃 차트 라벨 한글 글리프(NanumGothic 정적 폰트 번들,
                 # MIT, 순수파이썬 — airgap 안전). import만으로 font.family 설정됨

# services/shared·services/ingestion 공용(2026-08-11, 병합계획 문서 참고:
# 병합계획_komis-report-generator_260811.md)
pydantic>=2.12          # page_recommend/models.py의 Field(exclude_if=...)가 2.12+ 전용
pydantic-settings>=2.5  # services/shared/config.py
requests>=2.31          # geo/llm/openai_compat.py(services/shared/llm_client.py가 재노출)
pyhwp>=0.1b15         # HWP 구조 파서(services/ingestion/parsers/hwp.py)
opendataloader-pdf>=2.4  # PDF 파서가 import하는 geo.extractors의 의존성(Java 11+ 필요)
PyMuPDF>=1.24
easyocr>=1.7
pypdf>=4.0
numpy>=1.24

# 페이지추천(app/page_recommend, 2026-08-11 komis-report-generator-main search/ 이식).
# 외부 repo pyproject.toml은 langgraph>=1.0 + langgraph-checkpoint-sqlite>=3.0였는데,
# 후자(SQLite 체크포인터)는 채택하지 않았다 — 대화상태를 chat_session/chat_message에
# 두므로 필요 없음(page_recommend/service.py 주석). 실측: uv.lock이 고정한 1.2.9를
# python3.10에 설치해 그래프 실행까지 확인.
langgraph>=1.0
PyYAML>=6.0            # page_recommend/registry.py — 43개 페이지 정의 YAML 로드

# PageIndex(vendored, services/shared/pageindex_vendor — 2026-08-11) 의존성.
# services/shared/pageindex_client.py를 통해서만 쓴다(airgap 하드닝이 거기 있음,
# pageindex_vendor/README.md 참고). 버전은 원본 저장소 requirements.txt 고정판 그대로.
litellm==1.84.0         # 토큰 카운팅 — LITELLM_LOCAL_MODEL_COST_MAP=True 필수(원격fetch 차단)
openai>=1.70.0           # pageindex_lib가 이걸로 LLM 호출 — OPENAI_BASE_URL로 로컬 vLLM 라우팅
python-dotenv>=1.0       # pageindex_lib/utils.py가 import 시 load_dotenv() 호출
PyPDF2==3.0.1
pypdfium2==4.30.0
sortedcontainers==2.4.0

# MCP(rag/ragkit/mcp_server_public.py·mcp_server_private.py·mcp_client.py,
# 2026-08-26 public/private 물리적으로 분리된 모듈). 공식 SDK, 프로토콜
# 구현체라 자체 외부망 호출 없음(pageindex_vendor와 달리 vendoring 불필요)
# — 일반 pip 의존성으로 취급. **`<2` 고정 필수**(실측 확인, 2026-08-26 컨테이너
# 빌드에서 재현): mcp 2.x는 `FastMCP`를 `MCPServer`로 이름 바꾸고
# `mcp.server.fastmcp` 자체를 없애 mcp_server_public.py/private.py의
# `from mcp.server.fastmcp import FastMCP`가 ImportError로 죽는다(개발 로컬엔
# 이 세션 이전부터 1.18.0이 이미 깔려 있어 코드 실행 시엔 안 드러났다가,
# 컨테이너를 새로 빌드하면서 상한 없는 `mcp>=1.18.0`이 2.1.1을 끌어와 처음
# 재현됨 — 로컬에서 실측 통과했다고 컨테이너도 통과한다고 가정하면 안 되는
# 사례이기도 함).
mcp>=1.18.0,<2
