# -*- coding: utf-8 -*-
"""주기 실행(REPORT_SCHEDULE_CRON) → generator.py 호출 → out_report 저장 — 설계 단계
스켈레톤. dbio.write_df(df, "out_report", target, pk=["report_id"])로 적재
(dbio.py 재사용, §1)."""
raise NotImplementedError("설계 단계 스켈레톤 — 구현은 다음 세션(docs/CONTAINER_ARCHITECTURE.md §8)")
