# 비밀
.env
**/.env
*.key

# 파이썬
__pycache__/
*.pyc
**/.venv/
.ipynb_checkpoints/

# 대용량/생성 데이터 (커밋 금지)
*.duckdb
*.duckdb.wal
*.parquet
# 공유 DuckDB 볼륨 / geo 런타임
warehouse/
geo_data/
geo_data_2016plus_run/
warehouse/*.duckdb
warehouse/*.wal
# 수집·가공 산출
**/data/processed/
**/data/interim/
**/outputs/
geo/collectors/_gkg_masterfilelist_cache.txt

# 참조데이터(data/raw/hs_commodity_map.csv 등)는 위에서 무시하지 않으므로 기본 추적됨.

# 중첩 git (로컬에서 삭제했으면 무시됨)
mineral_supply_risk/.git/

# 2026-07-22: mine_ws 최상위(구 작업위치)에서 이관한 원본 참조자료(KOMIS·WoodMac·Argus·
# USGS·EU SCRREEN 등 제3자 보고서, 35GB) — git에는 부적합(대용량·저작권), 로컬 보관만.
# 우리가 작성한 산출물(claude_output/)만 추적한다.
documents/*
!documents/claude_output/
documents/dev/.git/
