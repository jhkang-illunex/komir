# -*- coding: utf-8 -*-
"""HWP → 마크다운 정규화. airgap 제약상 순수 파이썬 파서(예: pyhwp 계열, 사전 설치)
1차 시도 — LibreOffice headless 변환 경로는 이미지 크기 부담 커서 2차 후보
(docs/CONTAINER_ARCHITECTURE.md §5). mineral_supply_risk가 olefile을 이미 쓰고
있음(hwp 내부가 OLE 컴파운드 파일이라 관련 가능성 — 재사용 여부 확인할 것)."""
raise NotImplementedError("설계 단계 스켈레톤 — 구현은 다음 세션(docs/CONTAINER_ARCHITECTURE.md §8)")
