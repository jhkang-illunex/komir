# 챗봇 action r10 독립검증 (2026-09-21)

## 1차 실요청: 불합격

- 이미지: `komir-rag-chat:260921-action-r10`, `sha256:65d8b117a1e472296643b09ea9f90577e706a148b17f46fa989a638027ce4352`. 18002 테스트 컨테이너만 교체했다. 첫 빌드는 `app.routers.chat` 누락으로 기동하지 못해 폐기하고, r9 앱 누락 파일 복원·로컬 테스트 후 재빌드했다. `/healthz` 200을 확인한 뒤 요청했다.
- 요청 원문: 평가 XLSX `질문별 평가` 시트의 Q01·Q02·Q03·Q05·Q09·Q13·Q14·Q23→Q24→Q25·Q30, 그리고 VOBSERVED `2026년 1월부터 현재까지 리튬 월별 수입액과 아직 발표되지 않은 월을 구분해줘.`. 요청/원시 SSE/파싱 JSON은 `/tmp/sol-action-r10-260921/`에 보존했다. 모든 요청의 curl exit=0, SSE `done` 수신.
- 이미지 안팎 해시 일치: `rag_core/ragkit/action_contract.py` `97da876f…`, `rag_core/ragkit/chatbot_graph.py` `8fa5593b…`, `app/intent.py` `910d2279…`.

| 사례 | 1차 실제 결과 | 판정 |
| --- | --- | --- |
| Q02 | `source_unavailable`, 표·차트·인용 없음 | 통과. 전체/메이저/희소 지수와 달리 코발트별 광물종합지수 원천 없음. |
| Q05 | 응답, 표 4·차트 4·인용 4. 2026-07-07~09-08만 조회했고 구리·코발트 `[DEV_DUMMY] spot`과 니켈 `LME CASH`를 섞어 가격·변동률 제공 | **불합격**. 최근 1년의 세 광종 실가격 비교가 아님. 경고 문구만으로 더미 수치 표를 실데이터 답으로 인정할 수 없음. |
| Q09 | `source_unavailable`, 표·차트·인용 없음 | 통과. 현재 가동되는 8주 위기진단 점수 정본 없음. |
| Q13 | `source_unavailable`, 인용 없음 | 조건부 통과. 로그에 `aggregate_incomplete:komis_ranking`, 구리 수입 모집단 `DEV_DUMMY`, `advisor_rejected`가 기록됨. |
| VOBSERVED | `trade.monthly` 응답, 2026-06~09 리튬 더미 수입액 표·차트 제공; 01~05를 `미집계` 표기 | **불합격**. 실관측이 아닌 더미 수치이며 과거 미집계와 아직 발표되지 않은 월을 구분해야 함. |
| Q23→Q24→Q25 | Q23 응답. Q24는 니켈 06-17~09-08과 코발트 07-07~09-09를 같은 기간 비교로 출력하고 코발트 더미 사용. Q25는 `slot_unresolved` | **불합격**. Q25 필수 슬롯 오류가 남고 선행 비교도 기간·원천 계약 위반. |
| Q01·Q03·Q14 | 응답, 각각 표·차트·인용 있음 | 경로 회귀 정상. Q03은 더미 수입값이므로 실통계 품질 통과로 보지 않음. |
| Q30 | `out_of_scope`, 무인용 | 통과. 원문 `오늘 서울 날씨와 점심 메뉴를 추천해줘.`는 광물 업무 범위 밖. |

`source_unavailable`은 결과 문자열만으로 통과시키지 않았다. Q02의 지표 범위, Q09의 비가동 진단 정본, Q13의 집계 실패와 더미 원천을 대조했다. Q05·Q24·VOBSERVED는 실제 원천 부족이 있어도 더미 수치를 답으로 출력하여 실패다. Terra 보완 후 동일 원문·동일 세션 체인 재검증이 필요하다.

## r10b 보완본 실요청: 슬롯 해소, 최종 판정 보류

- 이미지 `komir-rag-chat:260921-action-r10b` (`sha256:07e5012e8ba84803b6f708017f70d2f8773028938a9faf2bafafde127e67dbf7`)를 동일한 18002 설정으로 재기동하고 `/healthz` 200을 확인했다. 이미지/작업본 해시는 `action_contract.py` `2bdf2bd1…`, `chatbot_graph.py` `54e936ad…`, `chatbot.py` `0fec8469…`, `app/routers/chat.py` `a29529c5…`로 각각 일치한다.
- 우선 12요청 원시 파일: `/tmp/sol-action-r10b-260921/`. Q02·Q05·Q09·Q13·VOBSERVED·Q24·Q25는 모두 `source_unavailable`로 표·차트·인용 없이 종료했고, `slot_unresolved`는 0건이다. Q01·Q03·Q14·Q23은 각기 표·차트·구조화 인용을 가진 답변, Q30은 날씨·점심 질문에만 `out_of_scope`였다. Q25는 동일 세션 Q23→Q24→Q25에서 선행 Q24의 `source_unavailable`를 받아 처리했다.
- Q05는 첫 실요청의 2개월 관측과 구리·코발트 더미 표본을 `source_unavailable:unverified_or_incomplete_observation`으로 차단했다. VOBSERVED 역시 리튬 월별 수입액의 `DEV_DUMMY` 경고가 있던 원천을 같은 경계로 차단했다. Q13은 로그의 `aggregate_incomplete:komis_ranking`, `MNRL0008`의 `DEV_DUMMY`, `advisor_rejected`와 일치한다.
- **Q24는 아직 pass가 아니다.** r10b 컨테이너 로그에 `aggregate_incomplete:komis_price_comparison`, `허용되지 않는 필터 값: '2026-06-'`가 기록됐다. 기간 필터를 원천 형식으로 투영하는 버그가 있어 실제 데이터 부재만으로 기권했다고 입증되지 않는다. Terra에 수정 요청했다.
- Q25 선행 상태 전파의 로컬 반례: 직전 assistant가 `source_unavailable`+`price.compare`이면 True이고, `trade.monthly` 기권, `slot_unresolved` 가격 비교, 더 오래된 가격 비교 기권 뒤 최신 범위 밖 응답은 모두 False였다. 구조화된 직전 가격 비교 실패에만 적용된다.

### 전체 30문항 회귀

동일 r10b 이미지에서 Q01~Q30 평가 원문을 재실행하고 Q23→Q24→Q25는 동일 세션으로 연결했다. 원시 SSE/JSON은 `/tmp/sol-action-full-r10b-260921/`에 보존했다. 30건 모두 curl exit=0과 `done`을 수신했으며, 답변 7건(Q01·Q03·Q04·Q07·Q12·Q14·Q23), `source_unavailable` 22건, `out_of_scope` Q30 1건, **`slot_unresolved` 0건**이다. 모든 기권 응답에는 표·차트가 없었다.

전체 기권 22건을 일괄 통과로 보지 않는다. Q17은 원문이 실자료가 없을 때 필요한 입력값과 계산식을 요구하므로 별도 검토가 필요하다. Q21·Q22는 `챗봇_public_내부지식_개념질의_대응_260921.md`의 source-first 정책상 직접 뒷받침하는 OKF·PageIndex 근거가 없으면 무인용 기권이 맞으며, 모델 내부 지식만으로 설명할 수 없다. Q28은 r9에서 가격 주장 반증을 답했는데 r10b에서는 기권으로 회귀했다. Q06·Q11·Q29도 r9 답변에서 기권으로 바뀌어 원천·더미 게이트 및 기간 경계를 각 문항별로 대조해야 한다. Q01·Q03·Q14·Q23의 라우팅은 정상이나, Q03의 더미 수입 순위 수치와 Q23 니켈 가격의 광종 수준 더미 경고는 실통계 품질 통과로 해석하지 않는다.

## r10c·r10d 추적

- r10c 이미지 `sha256:9d183a4693ff6c283348428b450406e9c8a7c8e18185c99814aa62089c1fdb63`의 전체 30문항 원시 파일은 `/tmp/sol-action-full-r10c-260921/`이다. 결과는 7답변·22 `source_unavailable`·Q30 `out_of_scope`, `slot_unresolved` 0건으로 r10b와 같았다. Q24의 이전 `허용되지 않는 필터 값: '2026-06-'` 로그는 사라졌으며, Q24·Q25는 무표·무차트·무인용 기권했다. VOBSERVED는 별도 `/tmp/sol-action-r10c-variants-260921/`에서 같은 기권을 재확인했다.
- Q28의 r9 수치 반증은 본문 자체가 더미 예시라고 경고했다. r10c 컨테이너의 DB 메타를 직접 읽은 `resolve_mineral_meta('MNRL0002')` 결과는 `('니켈', 'DEV_DUMMY')`다. 따라서 Q28의 실데이터 부족 기권은 통과다. Q01·Q23의 기존 니켈 수치 응답도 실통계 검증 성적에서 제외한다. 이 두 답변과 Q03의 더미 수치 출력은 명시 경고를 둔 기존 데모 동작이며, 전역 차단 여부는 이번 슬롯 수정과 분리한 품질 이슈다.
- Q17은 r10c에서 `document.retrieve`와 Advisor의 `retrieval_insufficient:리튬의 비축 현황에 대한 구체적인 수치나 정보가 담긴 근거가 없습니다` 판정으로 대안 계산 답변에 실패했다.
- r10d 이미지 `sha256:588d2aa303dbecfbc274096a0110589ae87c173b8a04eb5be0bca5343a5de0e7`의 우선 실요청 원시 파일은 `/tmp/sol-action-r10d-focus-260921/`이다. Q17 원문과 입력값·공식 변형 VSTOCK1은 **stage 1 `slot_unresolved`**로 새 회귀다. 실측값 부재를 직접 명시한 VSTOCK2만 `stockpile.methodology`로 계산식·계산 불가 조건을 답했고 정적 방법론 문서 인용 1건과 본문 `[1]`이 일치하며 실수치를 창작하지 않았다. Q17 원문과 VSTOCK1의 슬롯 오류는 필수 수정 대상으로 Terra에 전달했다. Q02·Q05·Q09·Q13·Q24·Q25·Q28·VOBSERVED는 무표·무차트·무인용 `source_unavailable`, Q21·Q22는 직접 근거 부족에 따른 무인용 기권, Q30은 원문상 실제 범위 밖 `out_of_scope`였다.
- r10e 이미지 `sha256:4d6cb296cc52df5116d71390570e1a37d22aa79c3221137fed1dd4074fe8cbed`의 우선 요청 원시 파일은 `/tmp/sol-action-r10e-focus-260921/`이다. VSTOCK1/VSTOCK2는 정적 문서 인용 1건으로 입력값·공식만 답했으나 **Q17 원문은 `ambiguous`로 기권**하며 기간·정보 유형을 되묻는 문구를 반환했다. 원문의 `자료가 없으면 필요한 입력값과 계산식을 설명` 조건을 무시하므로 아직 불합격이다. Q17 원문을 대상으로 추가 보정이 진행 중이다. 그 외 Q02·Q05·Q09·Q13·Q24·Q25·Q28·VOBSERVED 무인용 기권, Q21·Q22 source-first 기권, Q30 범위 밖 분류는 유지됐다.

## r10f 독립검증: Q06·Q10 계획 회귀 남음

- 이미지 `komir-rag-chat:260921-action-r10f` (`sha256:7fb9bf729760a86baf99b8cfa56a4ed4fabefe4951bdcaf71a470822db58bd7e`), 18002 테스트 컨테이너 `/healthz` 200. `action_contract.py` `fefbe91f…`, `chatbot_graph.py` `806d9e2b…`, `chatbot.py` `fdf568fd…`, `mcp_client.py` `2c1f058e…`, 비축 계산 문서 `6f1f4d40…`의 이미지/작업본 해시가 일치한다.
- 우선 원문·변형의 요청/SSE/JSON은 `/tmp/sol-action-r10f-focus-260921/`, 전체 평가 Q01~Q30과 Q23→Q24→Q25 동일 세션은 `/tmp/sol-action-full-r10f-260921/`에 보존했다. 우선 Q17 원문·VSTOCK1·VSTOCK2 모두 정적 문서 1건을 인용해 현재재고·목표재고·일평균소비량의 필요성과 단위, 부족량 `max(목표−현재,0)`, 비축일수 `현재/일평균소비량`, 분모 0·미확인 시 계산 불가를 설명했다. 비축 실수치는 출력하지 않았고 본문 `[1]`과 `done.citations[1]`이 일치했다.
- 필수 Q02·Q05·Q09·Q13·VOBSERVED·Q24·Q25는 `source_unavailable`로 무표·무차트·무인용 기권했다. Q05·Q24의 구리/코발트 더미 및 1년/동기간 불충족, VOBSERVED의 리튬 더미 월별 통관, Q02 광종별 종합지표 부재, Q09 주간 진단 정본 부재, Q13 집계 불완전/더미 수입국 원천, Q25 선행 Q24 비교 부재와 맞는다. Q24의 잘못된 날짜 필터 로그는 r10c 이후 사라졌다. Q21·Q22는 문서 근거 부족으로 무인용 기권했고, Q30 원문은 날씨·점심 메뉴라 `out_of_scope`가 적합하다.
- 전체 30건은 모두 curl exit=0·SSE `done` 수신. 답변 7건(Q01·Q03·Q04·Q07·Q14·Q17·Q23), `source_unavailable` 20건, `unsupported_combination` 2건(Q06·Q10), `out_of_scope` 1건(Q30), **`slot_unresolved` 0건**이다. Q06은 r9 답변에서 조합 차단으로 회귀했고 Q10도 계획 결과가 변동했다. `unsupported_combination`은 실제 출처 부족의 증거가 아니므로 이 두 사례를 불합격으로 기록하고 추가 수정을 요청했다. 30문항 전체 답변 품질 합격으로 판정하지 않는다.
- Q01·Q23의 니켈(`MNRL0002`) 원천 메타는 컨테이너 DB 직접 조회에서 `DEV_DUMMY`; Q03도 더미 통관 값이다. 이들의 답변은 기존 데모 경로 회귀 확인으로만 계산하고 실데이터 검증 성적에서 제외한다. 전역 더미 차단은 별도 품질 이슈로 남긴다.

## r10g 최종 재검증

- Q06·Q10 계획 정규화 보정 후 `komir-rag-chat:260921-action-r10g` (`sha256:0d664664231dc2b4643f3b96657895d02928ebdb2db1bcb491fe32b7ee72f386`)를 18002 테스트 컨테이너로 교체했다. `/healthz` 200. 이미지/작업본 해시가 `action_contract.py` `40fe2837…`, `chatbot_graph.py` `806d9e2b…`, `chatbot.py` `fdf568fd…`, `mcp_client.py` `2c1f058e…`, 비축 계산 문서 `6f1f4d40…`로 모두 일치한다.
- 전체 30문항의 요청·SSE·JSON은 `/tmp/sol-action-full-r10g-260921/`, VOBSERVED 변형은 `/tmp/sol-action-r10g-variants-260921/`에 보존했다. 30건과 변형 1건 모두 curl exit=0과 `done` 수신. 전체는 답변 8건(Q01·Q03·Q04·Q07·Q12·Q14·Q17·Q23), `source_unavailable` 21건, `out_of_scope` Q30 1건이며 **`slot_unresolved` 0건, `unsupported_combination` 0건**이다.
- Q06은 `price.compare` stage 3까지 진행한 뒤 리튬 더미/기간 부족으로 무표·무차트·무인용 `source_unavailable`이 됐다. Q10은 현행 위기진단 정본이 없는 요구를 stage 1 `source_unavailable`로 닫았다. r10f의 조합 차단 2건은 해소됐다.
- Q02·Q05·Q09·Q13·Q24·Q25·VOBSERVED는 앞선 실원천 부족/더미/선행 비교 부재에 맞춰 무표·무차트·무인용 `source_unavailable`이다. Q23→Q24→Q25는 같은 세션에서 실행했다. Q17은 `stockpile.methodology` 정적 문서 인용 1건으로 실수치가 없음을 밝히고 필요 입력값·단위·두 계산식·분모 불가 조건을 답했다. Q21·Q22는 직접 문서 근거 부족으로 무인용 기권, Q30은 날씨·점심 원문에 한해 `out_of_scope`다.
- **판정:** 사용자 지정 범위의 슬롯 미해결은 전체 30문항에서 해소됐고, 실데이터/직접 근거가 부족한 필수 사례는 수치를 꾸며내지 않고 기권했다. 범위 밖은 질문 원문을 확인한 Q30만 해당한다. 답변 8건 중 기존 데모 더미 수치 답변은 실데이터 품질 합격에 포함하지 않는다. 그 밖의 답변 내용·전역 더미 정책까지 이번 검증이 보증하지는 않는다.
