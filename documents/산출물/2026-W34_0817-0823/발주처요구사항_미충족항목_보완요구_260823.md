# 발주처 요구사항 미충족(갭) 목록 + 보완 요구사항

- 작성일: 2026-08-23
- 근거 문서: 과업지시서(`documents/260625 AI 기반 핵심광물 위기진단 및 수요예측 모델
  개발 과업지시서_일루넥스.pdf` 붙임1·붙임2) · 화면기획안 v1.3(`documents/기획문서/
  260731 핵심광물 수급위기 진단 화면 기획안 ver.1.3.pdf`) 및 그 대조 문서
  `documents/산출물/2026-W33_0810-0816/화면기획안_v1.3_기능단위_AI작업_구현현황_260813.md`·
  `인수인계서_TODO_대조_260813.md` · `documents/산출물/한양대/
  0818발표자료_komir구현_비교분석_260819.pptx` · `outputs/model_opt/
  lookahead_bias_audit_260813.md` · `한양대_질의사항_260823.md`
- 목적: komir 구현이 발주처 요구사항(과업지시서·화면기획안·신규 보안감사 요구)과 아직
  **매칭되지 않는 부분을 전수 목록화**하고, 각각에 대한 수정·보완 요구사항을 정리 —
  내부 우선순위 판단용
- 원칙: 이미 해소된 항목(예: 08-18 expanding-window 리팩터로 처리된 lookahead 구조문제)은
  제외하고, **2026-08-23 현재 시점 기준 미해소** 항목만 담았다. 코드 변경 없음(순수 점검).

---

## A. 과업1 — 위기진단 (과업지시서 붙임1·붙임2 대비)

| # | 현황(미충족) | 보완 요구사항 |
|---|---|---|
| A1 | **필수변수④ 세계공급부족(소비/공급) 미대응** — 과업지시서 명시 필수변수 6종 중 이 변수에 대응하는 피처가 코드에서 확인 안 됨(`fgap_ni` 등 유사 후보는 검정만 하고 미채택). | 세계 소비/공급 데이터 원천(USGS·WoodMac 등) 조사 → `r10_retune_harness`로 신규 피처 후보 검정 → 채택기준 통과분만 진단모델에 반영. 데이터 원천이 마땅치 않으면 발주처에 데이터 제공 요청. |
| A2 | **신규지표 3종(공급망압력지수·원자재지수·ESG지수) 미완성** — 과업지시서 명시 요구. 원자재지수만 시도 후 라벨오염 우려로 미채택, 나머지 2종은 미착수. | 공급망압력지수(GSCPI 등 공개지수 활용 검토)·ESG지수(광종별 공급국 ESG 스코어) 설계 착수. 원자재지수는 라벨오염 우회 설계(타깃과 분리된 파생 방식) 재검토 후 재시도. |
| A3 | **헤드라인 QWK(0.934)의 y_lag1(전월 라벨) 의존** — dQWK 0.808로 압도적 기여, `mart_weekly_diagnosis`의 교사(수급동향지표) 조인이 `avail_date` 없이 자기참조월로만 조인돼 KOMIS 실제 발행지연이 반영 안 됨. "지연=1개월" 가정에 성능이 낙관적으로 의존. | **발주처에 KOMIS 수급동향지표 실제 발행지연 일수 확인 요청**(코드로 해소 불가, 데이터 사실 확인 필요 — F1과 동일 항목). 확인되면 `weekly_mart.py`의 교사 조인에 실제 지연을 반영해 y_lag1 의존도 재평가. |
| A4 | **`geo_event.published_at` 컬럼 무의미(경미)** — `publish()` 1회당 단일 타임스탬프를 전체 이벤트에 일괄대입(distinct=1, 29.6만행 전부 동일값), 원래 의도(이벤트 발행일 추적)와 다른 용도불명 컬럼으로 방치. | 컬럼 용도 폐기 결정(다운스트림 미사용 확인됨) 또는 실제 발행일로 재계산 — 우선순위 낮음, 정리 작업으로 별도 처리. |

## B. 과업2 — 수요예측 (과업지시서 대비)

과업2 자체 요구(5광종·12개월·수입액+수입중량)는 komir가 문자 그대로 충족(비교 PPT
slide6~7·14 확인)해 이 절엔 **명시적 미충족 항목 없음**. 다만 화면기획안이 요구하는
확률 시나리오 프레이밍은 아래 C3에서 다룬다(모델 자체가 아니라 화면 표현계층 공백).

## C. 화면기획안 v1.3 AI기능(9건) 대비 — 미구현·부분구현 (2026-08-13 실측 기준, 08-23 재확인 결과 변경 없음 — 08-13 이후 관련 커밋 없음)

| # | 기능 | 현황(미충족) | 보완 요구사항 |
|---|---|---|---|
| C1 | #13 AI종합분석 및 관련뉴스 | **미구현** — 서술형 종합진단·광종별 1줄뉴스요약 생성 함수 자체가 없음(신규 LLM 파이프라인 필요). | 신규 LLM 파이프라인 설계(현황→원인→대응 서사 생성). 한양대 5요인 루브릭 프롬프트 설계 참고 가능(질의사항 §6 협업제안). `r10_retune_harness` 채택기준 조건부 도입. |
| C2 | #18 위험요인진단(SHAP 기여도+5유형판별) | **구현진행중** — 기여도 계산 로직(permutation importance·Ridge 계수)은 있으나 화면 스펙 6개 변수명 그대로의 %분해 API·5+1 위기유형 판별 로직 미확인. | 기존 `diagnosis.py`/`nowcast.py` 기여도 계산을 화면 스펙에 맞는 어댑터 API로 래핑, 5+1 유형 판별 규칙 신규 작성(재조립 위주, 상대적으로 빠름). |
| C3 | #20 이벤트전망(비관/중립/낙관 확률) | **구현진행중** — conformal 분위구간(q10/q90)·4단계 확률(`_stage_probs`)은 있으나 "3분류 시나리오" 프레이밍·확률 라벨링 로직 없음. | 신규 모델 불필요 — 기존 conformal 분위구간을 3구간 내러티브로 재포장하는 표현계층 작업만 필요(비교 PPT slide10에서 이미 확인된 저비용 경로). |
| C4 | #21 AI종합판단(자연어 코멘트) | **구현진행중** — `alert.py::_build_reasons`가 규칙기반 템플릿 문장만 생성, 자유서술형 LLM 종합판단 없음. | LLM 서술형 생성 레이어 신규 추가(C1과 유사 파이프라인 공유 가능) — `r10_retune_harness` 채택기준 조건부. |
| C5 | #29 챗봇 진입점/별도서버 | **구현진행중** — `services/rag_chat`가 물리적으로 분리된 배포단위라 방향은 맞으나 GNB 진입점 UI·공단 캐릭터는 프론트엔드 영역(이 저장소 범위 밖). | 프론트엔드 팀과 연동 확인만 필요 — 백엔드 조치 불필요. |
| C6 | #30 도입부 고정질문 4개 | **미구현** — 관련 코드 없음. | 고정질문 4개 정의(화면기획 문구 참고) → 프론트 초기화면에 노출, 백엔드는 일반 질의 경로 재사용 가능(경미). |
| C7 | #31 입력란 상태표출/에러 3분류 | **부분구현(백엔드만)** — SSE 스트리밍 이벤트는 있으나 에러 3분류(요청불명확/기술오류/정책제한) 텍스트 분류 로직 미확인. | 에러 발생 지점별로 3분류 태그 부여 로직 추가(예외 타입 매핑), 프론트 상태표출은 프론트 영역. |
| C8 | #32 근거데이터·데이터출처 버튼 | **부분구현** — `done` SSE 이벤트에 `citations`(source_path·section_heading)는 포함되나 "원본 메뉴로 이동" 프론트 연동은 별도. | 백엔드 데이터는 준비 완료 — `citations`→메뉴 라우팅 매핑 테이블만 프론트와 합의해 추가. |
| C9 | #33~34 데이터표출 분기·표기 5분기 | **미구현** — 표+차트/텍스트단답/메뉴링크 3분기, 데이터표기 5단계 판별 로직 코드 없음(현재는 텍스트 생성만). | 질문 유형 분류기 신규 작성(intent.py 확장) + 표/차트 이벤트 생성 로직(이미 SSE 계약엔 table/image 이벤트가 정의돼 있어 스트리밍 프레이밍은 재사용 가능). |
| C10 | #36 범위밖 질문 4분류 | **미구현** — 현재는 `generate.py`의 단일 `ABSTAIN_TEXT`뿐, 4분류(지원범위밖/광종밖/기간없음/모호)+대안제시 로직 없음. `page_recommend`의 `status` 4분류(recommended/ambiguous/not_found/needs_filter)와는 목적이 다름(혼동 주의). | 문서 Q&A 경로에 범위밖 판정 분류기 신규 추가, 각 분류별 안내문구·대안 광종 제시 로직 작성. |
| C11 | #37 멀티턴 대화(맥락유지) | **부분구현** — `session_store.py`로 이력 저장은 되나 RAG 문서QA 경로는 매 턴 독립 처리(맥락 미반영). `page_recommend` 경로만 이전 맥락 반영. | RAG 경로에도 `_history_for_graph`류 맥락 주입 추가(이미 있는 세션 저장 재사용, 신규 로직 최소). |
| C12 | 정형 통계조회 배선(우선순위 높음) | **미연결** — `retrieval/structured.py`에 `TEMPLATES`·`run_template()` 구현은 이미 있으나 `routers/chat.py`가 이 경로를 호출하지 않음(grep 0건). 답할 수 있는 코드가 있는데 배선만 안 된 상태. | `chat.py`의 intent 분류에 "정형 통계 질의" 분기 추가 후 `structured.py` 호출 배선 — **코드 재사용, 신규 로직 거의 불필요, 가장 저비용으로 해소 가능한 항목**. |

## D. 공통 인프라·근거계약 (komir·한양대 양쪽 다 미해결)

| # | 현황(미충족) | 보완 요구사항 |
|---|---|---|
| D1 | **DB/VDB 공통 근거계약 없음** — `structured.py`·`dense_pg.py`·`pageindex.py`가 각각 다른 모양의 결과를 반환, 단위·기준시점·출처를 통일하는 스키마 없음. | 공통 응답 스키마 설계(단위·기준시점·출처 필드 통일) — 어느 한쪽(komir 또는 한양대)이 먼저 설계해 공유하면 중복 작업 방지(인수인계서_TODO_대조 §3 권고). |
| D2 | **RAG 품질기준 미정의** — 최소 유사도점수·근거수·중복제거·재순위화 기준이 어디에도 정의돼 있지 않음. | 리트리버 평가 스크립트(`eval_retrieval.py`)로 기준값 실험 후 임계값 확정, 코드에 하드코딩된 관용값이 아니라 근거 있는 기준으로 문서화. |
| D3 | **챗봇 정형+비정형 혼합조회 조정 서비스 없음** — `mode=auto`가 "문서Q&A vs 페이지추천" 2択 분류만 하고, 한 턴 안에서 여러 도구를 섞는 조정(orchestration) 로직이 없음. | `chatbot_graph.py`의 도구선택 로직을 3자 조정(구조화조회+dense+pageindex 외에 페이지추천까지)으로 확장 검토 — 설계 규모가 커서 별도 사이클로 분리 권고. |
| D4 | **report_gen 분석요약 3종 미구현** — `/prices`(광물자원가격)·`/domestic-trade`(대한민국 수급지도)·`/global-trade`(글로벌 수급지도) 엔드포인트가 `501 NOT_IMPLEMENTED`로 남아있음(이식 원본 코드 자체가 미구현이라 komir도 동일). | 원본(`komis-report-generator`) 쪽 구현 완료 여부 추적 후 이식, 또는 komir 자체 구현 착수 여부 결정 필요(우선순위 판단 필요). |
| D5 | **대화 상태 정책 미정** — 사용자 귀속·보존기간·삭제 정책이 `CHAT_SESSION_TTL_DAYS` 설정값만 있고 실제 정리 배치는 없음. | 세션 만료 배치(cron) 신규 작성, 개인정보 보관기간 정책은 발주처 확인 필요(F 절 참고). |
| D6 | **조달청보고서 887건 전량처리 범위 미확인** — komir가 먼저 전량 처리했으나 이게 발주처 승인 범위(전체본 vs 주간시장동향만)와 일치하는지 사후 확인이 안 됨. | 발주처에 조달청 문서 사용 범위 확인 요청 — 범위 밖으로 판정되면 재작업(코퍼스 축소) 필요. |

## E. [신규] 발주처 LLM/임베딩 모델 보안감사 요구사항 대비 (2026-08-23 확인, komir 자체 미착수)

| # | 현황(미충족) | 보완 요구사항 |
|---|---|---|
| E1 | **`gemma-4-26b-a4b`(LLM) 보안 취약점 보고서 없음** — geo 이벤트 추출·GKG 관련성 판정·rag_chat 챗봇 전부 이 모델 사용 중이나 보안 취약점 보고서 작성 착수 전. | 모델 출처(HuggingFace 등)·라이선스·알려진 CVE·가중치 배포 경로 안전성을 조사해 보고서 작성. 발주처 감사 기준·양식이 확인되면 그에 맞춰 재작성. |
| E2 | **`intfloat/multilingual-e5-small`(임베딩) 보안 취약점 보고서 없음** — RAG dense 검색(pgvector `doc_chunk`) 임베딩 생성에 사용 중이나 동일하게 미착수. | E1과 동일 절차로 보고서 작성. |
| E3 | **제외 모델 지정 시 대체 계획 부재** — gemma-4-26b-a4b 또는 multilingual-e5-small이 "제외 모델"로 지정될 경우 사전 검토된 대체 모델이 없음. | 동급 LLM(예: 다른 오픈소스 sLLM)·임베딩 모델(예: 다른 다국어 임베딩) 후보를 사전 조사해 대체 계획 문서화(실제 교체는 제외 판정 시에만 진행). |

## F. 발주처 확인·결정 대기 (komir 코드로 해소 불가 — 정책·데이터 사실 확인 필요)

| # | 항목 | 관련 |
|---|---|---|
| F1 | KOMIS 수급동향지표 실제 발행지연 일수 확인 | A3 근본 해소에 필요. 장기 미해결 최우선 항목(2026-08-13부터). |
| F2 | `public.KO_*` 테이블이 텅스텐(MNRL0018) 데이터만 보유 — 발주 5광종(CU/NI/CO/LI/REE) 실데이터 부재로 report_gen 분석요약 5종(#28 제외) 활용 불가 | D4와 연계. 5광종 데이터 확보 시점 확인 필요. |
| F3 | GPU 인프라 사양·제공 주체 미확정 | `시스템_결정필요사항.md` 1-1~1-2, 한양대 질의사항 §5(보안감사)와도 연계(감사 대상 서빙 환경 확정 필요). |
| F4 | 조달청 자료 범위·정본 판별 기준 미확정 | D6과 동일 항목. |

---

## 우선순위 요약(권고)

1. **저비용·즉시 가능**: C12(정형조회 배선)·C3(확률 3분류 재포장)·C6(도입부 고정질문) —
   기존 코드/구성요소 재조립만으로 해소.
2. **발주처 확인이 먼저 필요(코드 작업 무의미)**: F1(발행지연)·F2(5광종 데이터)·F3(GPU)·F4(조달청 범위) —
   확인 전까지 관련 코드 작업은 보류 권장.
3. **신규 설계 필요(중기)**: A1·A2(필수변수④·신규지표3종)·C1·C4(LLM 서술형)·C9·C10(분기·범위밖 로직)·
   D1~D3(공통계약·조정서비스).
4. **신규 트랙(즉시 착수 권고)**: E1·E2·E3(보안 취약점 보고서) — 발주처 감사 일정이 확인되는 대로
   착수해야 지연 위험 최소화.

*본 문서는 순수 점검 목적이며 코드 변경은 하지 않았다. 각 항목의 상세 근거는 위 "근거 문서"
목록의 원본을 참고할 것.*
