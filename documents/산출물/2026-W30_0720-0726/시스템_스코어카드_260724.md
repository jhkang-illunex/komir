# 핵심광물 수급위기 진단·수요예측 시스템 — 스코어카드

작성일: 2026-07-24 (최신 버전 v1.5 — v1 최초 작성 후 같은 날 4-4 설명가능성 구현(v1.1)·적대적 감사 및 수정(v1.2)·4-3 재적합으로 2장 버전정합성 갭 완전 해소(v1.3)·y_lag1 의존 심층검토 6계열 전부 기각(v1.4)·LME재고 등 외부 직교데이터 확보·검정(v1.5))
목적: 5개 파이프라인 단계를 정량 지표로 계측하고, 이후 버전이 올라갈 때마다 이 문서를
갱신(맨 위에 새 버전 추가, 과거 버전은 아래에 보존 — WORKLOG와 동일한 일지 방식)해
성능 추이를 추적한다.
정본 근거: `docs/WORKLOG.md` 전체, 라이브 DB(`warehouse/minerals.duckdb`) 직접 조회
(2026-07-24 실측)

---

## 0. 시스템 구조 (2단 분할)

사용자 확정(2026-07-24): 시스템은 **2개로 분할**되며, 5개 파이프라인 단계는 이 2개
시스템에 다음과 같이 속한다.

```
시스템 ① 수집기 시스템
  └─ (정형) 관세청·ECOS·KOMIS 수집기 + (비정형) GDELT GKG 벌크 수집기
     ※ 차후 보안 모듈 추가 예정

시스템 ② 분석 시스템
  ├─ 2-1. 데이터 분석·전처리(GKG 관련성 필터링 등)
  │      ※ 수집기의 보안 모듈이 이 모듈에도 적용될 가능성 있음 — 염두에 둘 것
  ├─ 2-2. 지정학위기지수 생성기
  ├─ 2-3. 5종 광물 수급위기 진단기
  └─ 2-4. 5종 광물 1년후 수요·가격 예측기
```

**보안 모듈 관련 메모**: 아직 설계·구현 전이라 이 스코어카드에는 반영하지 않았다.
수집기 시스템에 먼저 추가될 예정이고, 분석 시스템의 2-1(데이터 분석·전처리)에도
동일 모듈이 적용될 가능성이 있다고 전달받았다 — 향후 버전에서 보안 모듈이 추가되면
각 모듈 항목에 "보안" 지표 행을 신설한다.

---

## 1. 숫자 읽는 법 — 단계마다 "건수"의 단위가 다르다 ⚠ 필독

이 문서에는 361,407 / 1,815,184 / 295,157처럼 자릿수가 비슷한 숫자가 여러 번
나오는데, **서로 다른 것을 세는 숫자**라 혼동하기 쉽다. 처음 보는 사람이 "왜 문서마다
숫자가 다르지?"라고 의문을 갖지 않도록 아래 변환 흐름을 먼저 명시한다(GDELT GKG
경로 기준, 2026-07-24 실측):

```
① 원본 파일        361,407개 zip 파일 (2.4TB, 2016~2026년, 15분 단위 1개씩)
                    ← 수집기 시스템의 산출물. "이벤트"가 아니라 "파일" 개수.
                       zip 1개 안에는 전 세계 모든 주제의 뉴스 레코드가
                       수천 건씩 들어있어 이벤트 건수와 직접 비교 불가.
   │ 파싱(zip을 열어 CU/NI는 GDELT 테마코드, CO/LI/REE는 키워드로 후보 추출)
   ▼
② 원본 후보 이벤트  1,815,184건 (파싱은 됐지만 아직 정제 전 — 관련성 28.6%,
                    즉 100건 중 71건은 실제로 무관한 기사가 섞여 있는 상태)
   │ 규칙기반 필터 4라운드 + LLM 재검증 2단계 (분석 시스템 2-1 모듈)
   ▼
③ 최종 이벤트       295,157건 (2-1 전처리 모듈의 최종 산출물, 관련성 99.5%)
                    ← 이후 모든 문서·이 표에서 "이벤트 건수"라고 하면 이 숫자.
                       "29.5만"·"295,157"이 바로 이것이다.
```

**정리하면**: "36만 1천"은 ①**수집기가 받아온 원본 파일 개수**, "29만 5천"은
③**전처리를 다 마친 뒤 실제로 쓰는 이벤트 건수**다. 같은 파이프라인의 서로 다른
두 단계를 각각 정확하게 나타낸 것이라 모순이 아니다 — 다만 자릿수가 비슷해서
헷갈리기 쉬우므로, 아래 표부터는 숫자 옆에 **무엇을 세는 숫자인지 괄호로 항상
명시**한다.

---

## 2. 한눈에 보기 (v1.3, 2026-07-24 기준)

| 시스템 | 모듈 | 버전 태그 | 핵심 지표 | 최신값(단위 명시) | 베이스라인 대비 | 최근 갱신 |
|---|---|---|---|---|---|---|
| ① 수집기 | GDELT GKG 벌크 | - | 원본 zip **파일** 수집 성공률 | 99.97% (361,407개 파일 / 마스터리스트 361,503개 파일 — **이벤트 건수 아님**, 위 1장 참고) | - | 2026-07-08 |
| ① 수집기 | 관세청 정형 | - | DB **행(row)** 수 | `raw_customs_annual` 232,001행·`raw_customs_monthly` 232,001행(각각 별도 테이블) | - | 2026-07-14 |
| ① 수집기 | ECOS | - | 수집 **시리즈 종류** 수 / 총 행수 | 2종(KR_gdp_real·KR_industrial_production) · 257행 | - | 실행시점 즉시 |
| ② 2-1 전처리 | GKG 관련성 필터 | 4라운드+LLM2단계 | 최종 **이벤트** 관련성(SRS 표본검증) | **99.5%**[97.1,99.9] (최종 이벤트 295,157건 기준 — 위 1장 ③) | 원본 이벤트 28.6% 대비 | 2026-07-24(노이즈보강) |
| ② 2-2 지수 | 지정학위기지수 | v2 + #8 시점정합성 | NB2 Brier score(**확률예측 오차**, 낮을수록 좋음, 광종별) | CU·NI·REE 개선/CO·LI 열세 | 상수강도 기준선 | 2026-07-22 |
| ② 2-3 진단기 | 수급위기 4단계 경보 | alert_rule_v1 | QWK(**등급분류 일치도**, -1~1, 1이 완벽) | **0.9687** | 지속성(Naive) 기준선과 완전동일(net_gain=0) | 2026-07-24(전체체인 재적합) |
| ② 2-4 예측기 | 수입 물량·단가 예측 | fc_hgb_v1 | 금액 WAPE(**예측오차율**, %, 낮을수록 좋음) | **22.9~15.4%** | 계절나이브 20.9~36.0% | 2026-07-24(FX버그 수정+재적합) |

> 지표 이름 옆 괄호는 "이게 뭘 재는 지표인지" 설명이다 — Brier score·QWK·WAPE는
> 서로 다른 단계(확률 예측/등급 분류/수치 예측)에 쓰는 서로 다른 통계량이라
> 절대 직접 비교할 수 없다(예: "QWK 0.97"이 "WAPE 20%"보다 낫다는 식의 비교는
> 성립하지 않음). 각 모듈 절(3~4장)에서 지표별로 더 자세히 설명한다.

⚠→✅ **버전 정합성 갭(v1에서 처음 확인, 2026-07-24 안에 전부 해소)**: 2-2(지수)는
2026-07-22~24에 여러 차례 변경(#8 시점정합성·이중노출 잔차화·오늘 적대적 감사의
국가명 정규화·scale_k v3 재앵커 등)됐는데, 그 변경을 소비하는 2-3(진단기)·2-4(예측기)는
한동안 최신 지수 변경분을 반영하지 못한 상태였다. **2-4(예측기)는 설명가능성 구현과
함께(v1.1) 재적합**(WAPE 갱신, 우열 판정 불변). **2-3(진단기)도 v1.3에서 재적합
완료**(mart_weekly_diagnosis 재빌드→nowcast→alert 전체 체인 재실행) — QWK는 0.9687로
전혀 변하지 않았는데, 이는 5-2 감사에서 이미 확인한 "QWK가 사실상 y_lag1(지속성)
복사값이라 지수가 바뀌어도 거의 안 움직인다"는 발견과 정합적이다(순수 지정학신호만
보는 GEO_ONLY_NO_LAG 지표는 정상적으로 반응). **이로써 2장의 버전 정합성 갭 전체가
해소됐다.**

---

## 3. 시스템 ① — 수집기

| 구성요소 | 방식 | 규모(실측, **단위 명시**) | 상태 |
|---|---|---|---|
| GDELT GKG(비정형) | `geo/collectors/gkg_bulk_download.py`, 5워커 라운드로빈 | **361,407개 zip 파일**(2.4TB, 2016~2026년, 마스터리스트 361,503개 파일 대비 실패 96건=0.03%) — ⚠ 이벤트 건수가 아니라 원본 파일 개수(1장 참고) | 완료(2026-07-08), 증분 수집 미자동화 |
| 관세청(정형) | `mineral_supply_risk/msr/collectors/customs_api.py` | DB **행(row) 수**: `raw_customs_annual` 232,001행·`raw_customs_monthly` 232,001행·`raw_customs_annual_bycountry` 39,962행(223개국) | 156개월 백필 완료(2026-07-14) |
| ECOS(정형) | `msr/collectors/ecos_api.py` | **시리즈 종류** 2종(KR_gdp_real·KR_industrial_production) · **행 수** 257행. **환율 시리즈 없음**(관련 데이터는 KOMIS 원본 CSV를 예측모델이 직접 읽는 별도 경로 — 2026-07-24 확인) | 최신 |
| KOMIS 원본파일 | `msr/collectors/komis_files.py` | 가격·재고·환율 등 CSV/xlsx **파일** 직접 로드(건수 미집계) | 수동 트리거 |
| USGS refdata | `geo/refdata.py`(2026-07-22 스크레이퍼 3종 버그 수정 후 최초 가동) | **릴리스(연도) 수**: 4개(2022·2023·2025·2026) 확보, 2017~2021은 USGS가 원천 미발행(구조적 한계)·2024는 USGS 서버측 404(우리 문제 아님) | 부분 가동(가용 릴리스만) |

**자동화 상태(정직하게 기록)**: `crontab -l` 직접 확인 결과 이 프로젝트 관련 상시
스케줄은 현재 **없음** — 07-14 관세청 월간 백필 완료 후 임시 crontab 항목을 정리
삭제했고(WORKLOG 2026-07-14), "collector 도커 수집서버 기동 + cron 등록(주간/월간)"은
WORKLOG상 아직 **계획 단계**(운영 배포 항목)로 남아 있다. `CLAUDE.md`의 "무인 가동 중"
표현은 파이프라인 코드 자체가 무인 실행 가능하다는 뜻이지, 지금 이 순간 스케줄러가
상시 돌고 있다는 뜻은 아니다 — 향후 버전에서 실제 cron/도커 상시화가 되면 이 표를
갱신할 것.

---

## 4. 시스템 ② — 분석 시스템

### 4-1. 데이터 분석·전처리 (GKG 관련성 필터링)

| 지표 | 값 |
|---|---|
| 원본 **이벤트**(정제 전, 파일 개수 아님 — 1장 참고) | 1,815,184건 |
| 최종 **이벤트**(정제 후) | 295,157건 |
| 관련성(SRS 무작위 표본 n=200건을 사람이 직접 읽어 판정) | 99.5% (95% 신뢰구간 [97.1%, 99.9%]) |
| 정제 단계 | 규칙기반 4라운드 → LLM 관련성 재검증(1차) → LLM 적대적 재검증(2차) |
| 최근 변경 | 2026-07-24 CO/LI/REE 동음이의어 노이즈 6건 보강(회귀 0건 확인) |
| 상세 문서 | `documents/산출물/2026-W30_0720-0726/GKG_필터링_프로세스_260724.md` |

### 4-2. 지정학위기지수 생성기

| 지표 | 값 |
|---|---|
| 정규화 방식 | tanh0_100(0~100, 50=중립, 절대스케일이라 발행값 불변), scale_k_by_commodity(v3, 2026-07-24 재앵커 — 구 v2는 07-15) |
| 점수식 성분 **개수** | 6개(severity·rel·conc·hhi_mult·sgn·imp_mult·conf_mult) — 2026-07-22 conf_mult 추가 |
| 이중노출(imp_mult) | 광종별 mult/resid **모드** 선택(2026-07-22 #4, CU에서 resid 채택 기준 트리거) |
| 시점정합성 | 2026-07-22(#8) 수정 — USGS 릴리스별 as-of 조인, lookahead bias 제거 |
| tanh 포화(극값 쏠림) 비중 | 주간 지수 중 ≤5 또는 ≥95인 것의 **비율**: 1.5~7.7%(광종별, v3 재앵커 직후라 5장 감사 이전 0.1~0.4%보다 상승 — P90=88 정의상 자연스러운 수준, REE가 가장 높음(7.7%)은 꼬리가 두꺼운 특성, 5장 참고) |
| NB2 Brier score(광종별, "우리모델/상수강도 기준선" 형식 — **낮을수록 좋음**, 기준선보다 작아야 개선. 2026-07-24 국가명버그 수정+scale_k v3 재앵커 후 재계산) | CU 0.046/0.047✓ · NI 0.048/0.053✓ · REE 0.207/0.475✓ · CO 0.210/0.195✗ · LI 0.113/0.108✗ |
| Isotonic 사후보정(확률 보정 기법) 전후 Brier·ECE | Brier 0.1193→0.1188, ECE(기대보정오차) 0.089→0.080 (둘 다 낮을수록 좋음) |
| 최근 변경 | 2026-07-22(시점정합성·이중노출 잔차화·confidence 가중), 2026-07-24(전처리 노이즈 보강 — 무변화 확인 / 적대적 감사로 국가명 정규화+scale_k v3 재앵커+NB2 수렴체크 반영, 5장 참고) |
| **설명가능성(2026-07-24 실측 추가)** | **높음, 이미 구현·가동 중.** ① 점수식 자체가 결정론적 곱셈 구조라 "이 주 지수가 왜 이 값인지"를 성분별로 완전히 분해 가능(성분=사실 근거: 발행처 신뢰도표·USGS 생산점유·한국 수입비중 등, 블랙박스 아님). ② `geo/wiki.py`가 광종×월 단위로 `geo_data/wiki/<광종>/<연도>/<월>.md`를 자동 생성 — 그 달의 지수·raw_score·이벤트 건수와 **기여 이벤트 전체 목록**(유형·방향·심각도·국가·원본 근거 인용문)을 표로 제공. 실측: 654개 파일 실제 존재·내용 확인(예: REE 2019-03, 이벤트 13건 전부 근거문구 포함). ③ **잔여 결함**: GDELT 유래 이벤트는 "출처" 열이 "-:nan"으로 비어있음(manifest 병합 대상이 아니라서) — 근거 인용문(마지막 열)은 살아있어 설명 자체는 가능하나 발행처 표기가 깨끗하지 않음, 개선 여지. |

### 4-3. 5종 광물 수급위기 진단기 (4단계 경보)

| 지표 | 값 |
|---|---|
| 모델 | Ridge(alpha=1.0, 광종 풀링+더미), model_version=`alert_rule_v1` |
| QWK(등급 분류 일치도, -1~1, **1에 가까울수록 좋음**. 2026-07-24 재적합 — mart_weekly_diagnosis→nowcast→alert 전체 체인 재실행, geo_index v3 반영) | 0.9687(지속성/Naive 기준선과 **완전 동일** — net_gain=0.0000, 07-22~24의 지수 변경(시점정합성·이중노출·국가명·재앵커)이 이 지표엔 전혀 영향 없음. 5-2 감사에서 확인한 "QWK는 사실상 y_lag1 복사값" 발견과 정합 — 지수가 바뀌어도 안 바뀌는 게 오히려 그 발견을 재확인시켜줌) |
| GEO_ONLY_NO_LAG(순수 지정학·무역 신호만, y_lag1 제외 — 지수 변경의 실제 영향은 여기서 봐야 함) | 전환주 적중률 챔피언 Ridge(풀링) 0.5385, QWK 0.4566(기준선=나이브, 5-2 감사 권고대로 지속성 지표와 분리 병기) |
| **보조 조기경보 신규 동작점(v1.5, LME재고 피처)** | Δ분류+재고(CU·NI 한정): 전환주 적중 7/18(상향 5/10), QWK 0.8386, FAR 0.13 — 기존 챔피언(고재현율 10/18·QWK 0.73) 대비 **고정밀 보완점**. 재고 피처 기여는 부트스트랩 95% CI [0.111, 0.556]로 유의(P=1.000). 재고 데이터가 없는 CO/LI/REE는 미적용(CO는 LME 무료 공개분 수집기 개발 시 확장 가능) |
| 경보 **행(row) 수**(현재 라이브 `out_diagnosis_alert` 테이블, 5광종×과거 전체 관측주 누적) | 정상 760행 · 관심 446행 · 주의 208행 · 경계 120행 · 심각 98행 (합계 1,632행, 07-17판 대비 소폭 재분배 — "지금 이 순간 5광종 상태"가 아니라 누적 이력) |
| 최근 발행(`generated_at`) | 2026-07-24 15:23 |
| 최근 관측일(`obs_date`, 가장 최근 주차) | 2026-06-08 |
| ✅ | 2026-07-22~24 지수 변경분 반영 완료(2장 갭 완전 해소, mart 재빌드→nowcast→alert 전체 체인 재실행) — QWK 자체는 무변화(위 참고), 최신 개별 경보는 CU 심각·CO/REE 주의·LI 관심·NI 정상(07-17판과 동일 패턴 유지). |
| **설명가능성(2026-07-24 실측 추가)** | **높음, 세 모듈 중 가장 성숙 — 이미 자연어 설명문을 자동 생성 중.** ① 모델 자체가 Ridge(선형)라 "피처값×계수=기여도"로 완전 가산분해 가능(트리·딥넷 대비 원천적으로 해석 쉬움). ② `out_diagnosis_alert.reason` 컬럼에 **완성된 한국어 설명문**이 실제로 들어있음 — 실측 예시(CU, 2026-06-08): "[CU·심각(Red)] 주요 생산국의 정세 악화... (산출근거: 수급위기지수 99/100(model)) 확률: 심각 62%, 주의 22%. 기여: y_lag1 +33.1, price_z52 +3.2, import_hhi -0.6. 관련 이벤트: 'First Quantum has cleared the way...'(Panama, sev 3.0/3)" — 등급 정의·산출근거 점수·단계별 확률분포·상위 기여 피처(부호 포함)·연관 지정학 이벤트 인용까지 한 문장에 전부 담김. ③ `evidence_json` 컬럼에 구조화 버전(전체 `stage_probs`·`contrib`·`override_applied` 등)도 동시 저장돼 있어 사람이 읽는 설명(reason)과 기계가 읽는 근거(evidence_json)를 둘 다 갖춤. **"설명가능한 컨텐츠 생성"이 이미 실제로 되고 있는 상태** — 발주처 보고서·대시보드에 그대로 인용 가능한 수준. |

### 4-4. 5종 광물 1년후 수요·가격 예측기

| 지표 | 값 |
|---|---|
| 모델 | 물량·단가 분리(HistGradientBoostingRegressor ×2, 재귀 h=1~12), model_version=`fc_hgb_v1` |
| 금액 WAPE(예측오차율, %, **낮을수록 좋음**. 156개월 학습표본, 2개 검증시점, 2026-07-24 FX버그 수정 후 재적합) | 22.9%/15.4% (계절나이브 기준선 36.0%/20.9% 대비 우위, 두 시점 모두 — 환율 피처가 처음으로 실제 반영되며 수치 이동, 결론은 불변) |
| MASE(기준선 대비 오차 배율, 1 미만이면 기준선보다 좋음) | 재귀 0.88(0.78/0.98) vs Direct 1.04 — 재귀 방식 채택 유지 |
| 80% 예측구간 **커버리지**(conformal, CQR — 실제값이 예측구간 안에 들어간 **비율**, 목표 0.80에 근접할수록 좋음) | conformal 보수화 후 0.77/0.87(평균 0.82) |
| 최근 발행(`generated_at`) | 2026-07-24(재적합 완료) |
| 최근 기준월(`base_date`, 예측을 시작한 기준 시점) | 2025-12-01 |
| ✅ | 2026-07-22 지수 변경분 반영 완료(2026-07-24 재적합) — 2장 갭 해소 |
| **설명가능성(2026-07-24 구현 완료)** | **높음 — v1에서 "미구현"으로 확인했던 갭을 같은 날 해소.** `msr/models/forecast_unit.py`에 SHAP `TreeExplainer`(개별 예측 로컬설명) + `permutation_importance`(전역 중요도, `HistGradientBoostingRegressor`는 `feature_importances_` 속성이 sklearn 공식 한계로 없어 표준 대안 채택) 구현, `out_import_forecast_unit`에 `reason`(자연어)·`explain_json`(구조화) 컬럼 신규 추가. 실측 예시(CU, target_month=2026-01, h=1): "[CU 2026-01-01 h=1개월] 예측 기여요인(SHAP, 로그스케일) — 물량: 1개월 전 실적+2.0727, 광종 고정효과(LI)+0.3512, 3개월 전 실적+0.1181 / 단가: 1개월 전 실적-0.5968, 최근 3개월 이동평균+0.3666, 광종 고정효과(NI)+0.1141." — 발행에 실제 쓰인 모델·피처행을 그대로 재사용(재적합 없이 SHAP만 계산)해 "이 숫자가 왜 나왔는지"를 진단기와 대등한 수준으로 설명. **잔여 개선점**: 광종 풀링모델이라 "광종 고정효과(LI)"처럼 다른 광종 더미가 상위 기여로 잡히는 경우가 있음(수치적으로는 정확하나 비전문가에게는 직관적이지 않음 — 문구 다듬기 필요), permutation_importance는 in-sample(학습데이터 기준)이라 엄밀한 held-out 검증은 아님(explain_json에 명시해둠). |

### 4-5. 설명가능성 — 3개 모듈 종합 비교(사용자 요청, 2026-07-24)

| 모듈 | 현재 수준 | 근거 형태 | 실제 가동 여부 | 컨텐츠 생성 타당성 |
|---|---|---|---|---|
| 4-2 지정학위기지수 | 높음(가동 중) | 결정론적 수식 분해 + 이벤트별 원문 근거 인용 | ✅ `geo/wiki.py`, 654개 파일 실존 확인 | 이미 실현됨 |
| 4-3 수급위기 진단기 | 매우 높음(가동 중) | 자연어 문장(등급 정의+산출근거+확률분포+피처기여도+연관 이벤트 인용) | ✅ `out_diagnosis_alert.reason`/`evidence_json` 실데이터로 확인 | 이미 실현됨, 발주처 보고서에 즉시 인용 가능한 완성도 |
| 4-4 수요·가격 예측기 | 높음(같은 날 구현·가동) | 자연어 문장(SHAP 상위기여 요인, 물량·단가 각각) + 구조화 JSON(로컬+전역 중요도) | ✅ `out_import_forecast_unit.reason`/`explain_json` 신규 컬럼, 60행 실데이터로 확인(2026-07-24) | **당일 개발·반영 완료** — 4-3보다 뒤늦게 출발했으나 같은 세션에서 갭 해소 |

**총평**: 5개 모듈 중 "설명가능한 컨텐츠 생성"이 요구되는 3개(지수·진단·예측) **전부**
지금 실제로 가동 중이다 — v1 작성 당시(2026-07-24 오전) 예측기만 미구현이었던 갭을
같은 날 안에 해소했다. 발주처에 "설명가능성 확보"를 보고할 때 세 모듈 모두 실측
근거(파일·컬럼·실행 로그)를 인용해 보고할 수 있다.

---

## 5. 적대적 감사(사용자 요청, 2026-07-24) — 모델 구성·피처 적합성·오버피팅

**방법**: 3개 모델링 모듈(4-2·4-3·4-4)마다 독립 감사관(서브에이전트)에게 "이 설계가
틀렸다면 어디서 틀렸을까"를 최대한 의심하며 코드·실제 데이터를 직접 재현해 근거를
찾도록 지시(짐작 금지, 실제 코드라인·쿼리결과로만 보고). 심각도가 높은 발견은 이
문서에 적기 전에 별도로 직접 재현·검증(아래 "직접 재검증" 표시)한 뒤에만 반영했다.
분류: **✅수정완료**(당장 고쳐서 오늘 반영) / **⚠구조적 한계**(지금 못 고침, 데이터
축적·발주처 합의 등 필요) / **☑문제없음**(의심했으나 실측상 정당함 확인, 손대지
않음).

### 5-1. 지정학위기지수(4-2)

| # | 발견 | 관점 | 판정 | 대응 |
|---|---|---|---|---|
| 1 | **국가명 불일치로 CO 이벤트 44.6%(DRC 계열 표기 2,017건)가 USGS 최대 생산국 "Congo (Kinshasa)"(가중치 1.69)와 매칭 안 되고 conc=1.0(중립)으로 폴백** — 가장 집중된 광종의 가장 중요한 리스크가 조용히 무력화돼 있었음. REE의 "Myanmar"(48건, USGS 표기 "Burma")도 동일 패턴. | ② 피처 적합성 | ✅수정완료 | **직접 재검증**(DB 쿼리로 국가명 분포·USGS 가중치 실측 일치 확인) 후 `geo/indexer.py`에 국가명 별칭 정규화(DRC 5종 표기+Myanmar→Burma) 추가, conc/hhi 조인에만 적용(원본 `country` 값은 보존). |
| 2 | **scale_k(P90=88 절대앵커, v2/07-15)가 그 후 여러 변경(GKG 재정제·conf_mult·hhi_mult 실가동·위 #1 수정)으로 무너져 광종 간 "0~100 비교가능" 약속이 깨짐** — 수정 전 실측 P90 지수: CO 95.8(#1 수정으로 더 상승)·LI 82.7·REE 68.7·NI 65.0·CU 62.9(목표 88). | ③ 오버피팅(캘리브레이션 드리프트) | ✅수정완료 | **직접 재검증**(현재 raw_score 분포로 P90 재계산) 후 v3 재앵커: CU 78.7·NI 38.9·CO 21.9·LI 9.4·REE 13.4로 갱신, 재실행 검증 결과 5광종 전부 idx(P90)=88.0 정확히 복원. `GEO_INDEX_VERSION` v2→v3. |
| 3 | `geo/prob_model.py`의 NB2 MLE가 `isfinite`·`α>0`만 검사하고 **`mle_retvals["converged"]`를 확인하지 않아 실제로는 수렴 못 한 파라미터가 그대로 채택되는 경우 발견**(실측: CU 전기간 적합이 미수렴인데 α=0.396으로 채택돼 있었음 — REE α 붕괴와 같은 근본원인의 다른 증상). | ③ 오버피팅 | ✅수정완료 | `_fit_one()`에 수렴 체크 추가, 미수렴 시 기존 Cameron-Trivedi 모멘트 폴백 경로로 유도(2줄 수정). |
| 4 | `conf_mult`(confidence 가중, #7)가 실측상 준상수(0.6~0.8에 89.5% 집중, sd/mean 3.5%)이고 severity와 상관 0.59로 약한 이중계상 소지. | ① 구성 | ☑문제없음(과장 주의) | 감쇠폭이 최대 30%뿐이라 실해는 미미(ablation Jaccard 0.9+와 정합, 손대지 않음) — 다만 발주처 문서에서 "LLM 불확실성 실질 반영"으로 과대포장하지 말고 명목적 반영임을 명시. |
| 5 | `hhi_mult`가 광종 내 고유값이 릴리스 수(4~5개)뿐이라 사실상 광종별 상수에 가까움(conc와의 상관 0.007~0.14로 이중계상 우려는 기각). | ① 구성 | ☑문제없음 | 제거 불필요(향후 HHI 급변 시 신호가 될 설계 여지), #2 재앵커로 스케일 교란도 흡수됨. |
| 6 | `burst_k`(급증 임계, REE=4·CO=8)가 소표본 극단치에 좌우되는지 의심했으나, top10 제거·부트스트랩 500회(REE 74%가 그대로 4)로 안정성 확인 — 대신 **REE는 test(2024+) 구간 burst 적중률이 63%(train 13.9%)로 "이례적 급증" 임계의 의미 자체가 변질(체제전환)** — REE Brier 열세의 구조적 원인. | ③ 오버피팅 | ⚠구조적 한계 | 지금 재추정하면 lookahead 문제 발생. rolling 재추정 설계는 별도 과제, 우선 문서에 "REE burst 임계는 최근 체제와 안 맞을 수 있음"으로 한계 명시. |
| 7 | 발행처 신뢰도(rel)는 GDELT가 96.9%(rel=0.7)를 차지해 사실상 전역 상수 스케일러 — 차별화는 3.1%에만 작동(#3의 "재산정 불필요" 결론과 모순 없음, 효과범위가 매우 좁다는 걸 재확인). | ② 피처 적합성 | ☑문제없음 | 왜곡은 아니고 "기대효과가 원래 작다"는 것 — 문서에 효과범위만 명시. |

**재검증**: 위 1·2·3 수정을 전부 반영해 `geo index`→`geo prob`→발행까지 재실행.
NB2 Brier는 CU 0.046/NI 0.048/REE 0.207/CO 0.210/LI 0.113(수정 전 대비 ±0.002 이내,
광종별 우열 판정 CU·NI·REE 개선/CO·LI 열세는 불변) — **국가명 버그·재앵커라는 꽤 큰
수정에도 최종 확률 예측 품질은 안정적으로 유지됨을 확인.**

### 5-2. 5종 광물 수급위기 진단기(4-3)

| # | 발견 | 관점 | 판정 | 대응 |
|---|---|---|---|---|
| 1 | **타깃 순환성 실재 확인**: 운영 타깃(`teacher_supply_demand` 기반 crisis_index)과 가격(ref_price) 상관이 광종별 −0.84~−0.99로, 사실상 가격의 단조변환. 그런데 입력 피처에도 `ref_price`·`volatility_12w`·`spread_pct`·`price_z52` 등 가격 파생치가 포함 — 코드 주석("KOMIS 외생지표라 순환 아님")은 출처 기준으로만 참, 내용 기준으로는 순환. `documents/CLAUDE.md`의 최초 설계 경고("진단 타깃 순환성 주의")가 지금도 유효함이 실측으로 확정됨. | ② 피처 적합성 | ⚠구조적 한계 | KOMIS가 정답 자체를 가격 기반으로 정의하는 한 완전 제거 불가. **당장 가능한 건 보고 문안 교정**: "가격과 독립적인 AI 진단"이 아니라 "가격기반 상태지표의 nowcast + 외생신호 보조"로 정확히 기술, 예측력 주장은 순수 외생 신호만 쓴 GEO_ONLY_NO_LAG 결과(전환주 적중 ~0.5)로 한정. |
| 2 | **QWK 0.92~0.97대는 대부분 지속성(y_lag1) 복사값** — "직전 등급을 그대로 유지"만 해도 월간 QWK 0.9047, 주간 0.956~0.969로 모델 보고치와 사실상 동일. y_lag1 단독 Ridge가 11피처 전체보다 테스트 MAE가 더 낮음(나머지 피처 전부 음의 증분). y_lag1 포함 시 전환주 적중률은 학습모델 6종 전부 0.000. | ② 피처 적합성 | ⚠구조적 한계(문서화로 대응) | 등급이 원래 천천히 바뀌는 건 정당한 특성이라 y_lag1 자체는 유지. **당장 가능**: 모든 대외 보고 지표를 (QWK, 지속성 대비 순개선, 전환주 적중률) 3종 세트로 고정해 QWK 단독 인용을 금지. **07-24 심층검토(사용자 지시, v1.4)**: 미착수 대안 6계열(비대칭 상향게이트·Δ타깃 전환분류·서수모델·전환가중학습·잔차회귀·동역학/방향별 이벤트 피처)을 동일 워크포워드로 일괄 백테스트 — **전부 기각(일곱 번째 동일 결론)**. 형식 기준을 유일하게 통과한 전환가중 HistGBM(w=20)도 강건성 검증(가중 세밀스윕·부트스트랩 2,000회)에서 "전환 1건당 오경보 ~11건 + QWK CI 하한이 허용선 침범"으로 비권고. 크게 개선돼 보였던 방향별 이벤트 카운트 피처(전환주 적중 0.50)는 ablation 결과 **가격뉴스 반향(라벨 오염)**으로 판명(supply 계열만은 +전환 1건 수준). 결론: 병목은 모델 구조가 아니라 피처 정보량 — LME 재고·선물커브 등 외부 신규 직교 데이터 확보 전에는 이 방향 재시도 무의미. 상세: `mineral_supply_risk/outputs/model_opt/diagnosis_ylag_deep_review.md`. |
| 3 | 평가 표본이 협소: 2023·2024 폴드는 5광종 전원 단일클래스라 pooled QWK가 사실상 2025~ 폴드 하나로 결정, 전환주 27건, '심각' 라벨 광종당 3건(라벨 분포가 앵커 분위수 구성상 기계적으로 ~5%가 됨). train/test 분할 자체는 워크포워드로 결백 확인. | ③ 오버피팅 | ⚠구조적 한계 | 데이터 축적 필요. 당장은 QWK 소수점 셋째 자리 비교를 지양하고 부트스트랩 신뢰구간을 병기(WORKLOG에 이미 있던 미이행 권고 재확인). |
| 4 | Ridge(alpha=1.0)+광종 풀링 자체가 잘못됐는지 의심했으나, **광종별 분리학습을 직접 재현하니 오히려 파국**(2025~ 테스트 편향 CU +19.4·REE −30.5 vs 풀링 MAE 2.9~5.2, VIF 최대 3.6). | ① 구성 | ☑문제없음 | 풀링이 소표본(REE·CO)을 구제하고 있음이 실측 확인됨 — **손대지 말 것**. |
| 5 | `evidence_json`의 `contrib`(피처 기여도) 계수가 재학습 시점마다 크게 흔들림(import_hhi 1.88→0.06, spread_pct 부호반전) — 안정 피처(y_lag1·가격계열)만 안정적. geopolitical_risk 계수 부호가 광종별로 이질적(CU +0.31 vs LI −0.51)인데 풀링이 하나로 강제해, "지정학위험 상승"이 경보 사유에 "위기 완화 기여"로 표시될 수 있는 경우 발견. | ① 구성/XAI | ⚠재검토 필요(당장 가능) | 다음 버전에서 경보 사유(`reason`) 문안에 불안정 피처는 부트스트랩 CI 통과 시에만 노출하는 필터 추가 검토(4-4 예측기의 SHAP 안정성 문제와 유사한 XAI 신뢰도 이슈로 함께 관리). |

### 5-3. 5종 광물 1년후 수요·가격 예측기(4-4)

| # | 발견 | 관점 | 판정 | 대응 |
|---|---|---|---|---|
| 1 | **환율(fx) 피처가 100% 결측 — 모델이 환율을 한 번도 실제로 본 적 없음**. `skiprows=2`로 CSV 실제 헤더행("날짜,주간평균,기준")이 데이터 첫 행으로 들어와 날짜 파싱이 예외 발생 → 조용히 삼켜져 fx 전체가 상수 NaN. 도크스트링·`FEAT_LABELS`·`basis` JSON은 계속 "원달러 환율 반영"을 주장하고 있었음(사실과 다른 기재). | ② 피처 적합성 | ✅수정완료 | **직접 재검증**(제 환경에서 동일 예외 재현 확인) 후 `skiprows=3`+`errors="coerce"`+결측행 제거로 수정, 결측률 50% 초과 시 경고 로그 추가. 수정 후 실측: CSV 보유구간(2021-06~) 결측률 0%(수정 전 100%) — `reason`에 실제로 "원달러 환율" 기여가 나타나기 시작함(예: LI 물량 -0.09, 단가 +0.05). 2013~2021-05는 CSV 자체가 없어 여전히 결측(구조적 한계). |
| 2 | `HistGradientBoostingRegressor(max_iter=300)`에 `early_stopping` 미지정 — sklearn 기본값 `'auto'`는 표본>10,000에서만 발동해 775행에선 꺼진 채로 300트리 전부 학습되는지 의심. | ③ 오버피팅 | ☑문제없음 | 실측 `n_iter_=300`(꺼진 것 확인)이나, max_iter 100→300 OOS 민감도 비교에서 열화 없음(오히려 미세 개선) — 로그공간+얕은 트리(depth=4)에서 성능이 saturate. 계산낭비만 있고 해는 없음, 손대지 않음. |
| 3 | SHAP 결과 lag1(1개월 전 실적) 기여가 압도적이라 "단순 최근값 복사(persistence)"인지 의심 — **"마지막 관측 유지" 나이브와 직접 비교한 결과 명확히 우위**(ton WAPE 47.7→30.1·34.1→19.7, h=1만 보면 42.2→8.5). lag1 부호가 광종 레벨 차이를 흡수하는 역할(CO/REE 음수, CU/NI 양수)임도 확인 — 단순 복사가 아님. | ② 피처 적합성 | ☑문제없음 | 우려했던 것과 달리 모델이 실질적 가치를 더하고 있음이 실측 확인됨, 손대지 않음. |
| 4 | LME가격·지정학지수·환율 등 외생피처의 permutation importance가 lag1 대비 300배 이상 작고(ton: lag1 10.54 vs lme_l 0.034), geo_f는 top5 밖 — 도크스트링의 "시나리오 입력으로 교체 가능" 주장이 사실상 공허(교체해도 예측이 거의 안 변함). 결측도 큼(geo_f 21.9%, LI lme 60개월 결측). | ② 피처 적합성 | ⚠구조적 한계 | 월간 물량에 외생신호가 실제로 약할 가능성 + 결측 문제라 당장 모델 재설계로 해결 안 됨. **당장 가능한 건 문서 정정**: 발주처 자료에서 "시나리오 대체 가능" 주장을 빼거나 한계를 명시. |
| 5 | 재귀(recursive) h≥2 예측에서 SHAP의 "lag1"이 실제로는 **모델 자신의 직전 예측값**인데 `reason` 문구는 "1개월 전 실적"으로 표기(h=12까지 그대로) — 설명문이 사실과 다르게 나갈 수 있음. `cc_광종` 더미가 다른 광종 예측에도 상위 기여로 자주 등장해 비전문가에게 혼란(4-4 잔여개선점에 이미 일부 기록). | ①구성/XAI | ⚠재검토 필요(당장 가능) | h≥2 재귀 예측의 reason 라벨을 "직전 예측값"으로 분기 처리, `cc_` 더미는 top_n 선정에서 제외하는 방향으로 다음 버전에서 수정. |
| 6 | 워크포워드 백테스트(origin 2024-06/12)·conformal 보정원점(2022-06/12)의 미래누수 여부 — 학습 컷오프가 `month<=base_month`로 정확히 제한됨을 코드로 확인, 보정원점의 예측창도 평가원점보다 전부 과거. 경미하게 계절나이브 결측 대체가 평가구간 실측 평균을 쓰는 소규모 누수가 있으나 **베이스라인에 유리한 방향**이라 모델 우위 주장엔 보수적. | ③ 오버피팅 | ☑문제없음 | 방치 가능(모델에 유리하게 작용하는 누수가 아님). |

**재검증**: 위 1 수정 반영 후 재실행 — 금액 WAPE_decomp 22.9%/15.4%(수정 전 18.3%/
18.5%, 계절나이브 기준선 36.0%/20.9% 대비 우위는 두 시점 모두 유지), MASE 재귀 0.80
vs Direct 0.85(재귀 채택 유지). 수치는 이동했지만(실제 환율 신호가 새로 들어왔으니
당연) **결론(계절나이브 대비 우위, 재귀 방식 우세)은 안정적으로 유지.**

---

## 6. 다음 버전(v2)에서 할 일 — 이번 v1 작성 중 발견한 항목

1. ~~4-3(진단기)·4-4(예측기) 재적합~~ → **둘 다 2026-07-24 해소**(v1.1에서 4-4,
   v1.3에서 4-3). 4-3은 QWK 자체는 0.9687로 무변화였지만(y_lag1 지배 때문 — 5-2
   감사와 정합), GEO_ONLY_NO_LAG 지표(순수 지정학신호)는 정상적으로 반응 확인.
2. **4-2 민감도 분석 재실행**: 점수식 성분이 5개→6개로 늘었는데(`docs/DATA_REGISTRY.md`
   기록) 아직 6성분 기준 민감도(±30% 섭동) 재검증이 안 됨.
3. **수집기 상시화**: cron/도커 상시 가동이 계획 단계 — 실제 배포되면 "가동률/최근
   실행 성공 여부"를 이 표에 지표로 추가.
4. **보안 모듈**: 설계 확정되는 대로 ①수집기·4-1 전처리에 지표 행 신설(예: 접근제어
   커버리지, 취약점 스캔 결과 등 — 아직 형태 미정).
5. **CO/LI(지수 확률화 NB2) 열세**: 표본이 작아(burst_k 8/16, 즉 급증 판정 기준이 되는
   주간 이벤트 임계값 자체가 작음) 발생하는 구조적 한계로 이미 파악됨(WORKLOG) — 표본이
   자연 누적되면서 개선되는지 다음 버전에서 추적.
6. ~~4-4(예측기) 설명가능성 신규 개발~~ → **2026-07-24 완료**(4-4·4-5 참고, SHAP
   TreeExplainer + permutation_importance 구현·가동 확인).
7. **4-2 wiki.py 출처 표기 결함**: GDELT 유래 이벤트가 "출처" 열에 "-:nan"으로 표시되는
   문제(manifest 병합 대상 아님) — 근거 인용문 자체는 살아있어 치명적이진 않으나 정리
   권고.
8. **4-4 설명가능성 잔여 개선**: "광종 고정효과(LI)" 같은 더미변수 기여가 비전문가에게
   비직관적으로 보일 수 있음(수치는 정확) — 문구 다듬기 검토. permutation_importance가
   in-sample(학습데이터 기준)이라 엄밀한 held-out 전역중요도는 아님(explain_json에
   명시는 해뒀음) — 필요시 held-out 분리 재계산 검토.
9. **(5-2/5-3 감사 신규)** 4-4 재귀 h≥2의 `reason` 문구가 "1개월 전 실적"이라고 쓰지만
   실제로는 모델 자신의 직전 예측값(재귀 특성상 불가피) — h≥2에서는 "직전 예측값"으로
   라벨 분기, `cc_` 더미는 top_n 선정에서 제외.
10. **(5-2 감사 신규)** 4-3 경보 사유(`reason`/`evidence_json`)의 피처 기여도가 재학습마다
    불안정(import_hhi 30배 변동 등 실측) — 부트스트랩 CI 통과한 안정 피처만 사유 문안에
    노출하는 필터 검토, 4-4의 재귀 라벨 문제와 함께 "XAI 신뢰도" 카테고리로 관리.
11. **(5-2 감사 신규)** 4-3 대외 보고 문안 정정: 진단 타깃-피처 순환성(가격 상관
    −0.84~−0.99 실측)을 반영해 "가격과 독립적인 AI 진단"이 아니라 "가격기반 상태지표
    nowcast + 외생신호 보조"로 표현 교정, QWK 단독 인용 대신 (QWK·지속성 대비 순개선·
    전환주 적중률) 3종 세트 고정.
12. **(5-1 감사 신규)** 4-2 REE burst_k=4의 급증 판정 기준이 최근 체제(2024+)와 괴리
    (test 적중률 63% vs train 13.9%)돼 있다는 한계를 발주처 문서에 명시, rolling
    재추정 설계는 별도 과제로 검토.
13. **(5-3 감사 신규)** 4-4 도크스트링·발주처 문서에서 "LME·지정학지수·환율 시나리오
    입력으로 교체 가능" 주장을 재검토 — 실측 permutation importance상 외생피처
    영향력이 lag1 대비 300배 이상 작아 시나리오 교체 효과가 사실상 미미함을 반영해
    문구 수정 또는 한계 명시.

---

## 7. 버전 이력

| 버전 | 날짜 | 요약 |
|---|---|---|
| v1 | 2026-07-24 | 최초 작성. 2단 시스템 구조 확정, 5개 모듈 현재 지표 수집. 4-3·4-4가 4-2의 최신 변경분을 반영 못한 갭 발견. 사용자 피드백(GKG 파일 개수 vs 이벤트 건수 혼동) 반영해 "숫자 읽는 법"(1장)과 표마다 단위 명시를 추가. 사용자 요청으로 4-2·4-3·4-4에 "설명가능성 및 설명가능한 컨텐츠 생성 타당성" 항목 추가(4-5 종합비교 신설) — 지수·진단은 이미 가동 중, 예측만 신규개발 필요임을 실측 확인. |
| v1.1 | 2026-07-24 | 같은 날 내 갭 해소 2건 반영. ① **4-4 설명가능성 신규 구현**: `msr/models/forecast_unit.py`에 SHAP TreeExplainer(로컬)+permutation_importance(전역, `HistGradientBoostingRegressor`는 `feature_importances_` 미지원이라 표준 대안) 추가, `out_import_forecast_unit`에 `reason`·`explain_json` 컬럼 신규 — 60행 실데이터로 가동 확인, 4-5 종합비교표 예측기 ❌→✅ 갱신. ② **4-4 재적합 부수효과**: 같은 실행에서 07-22 지수 변경분이 자동 반영돼 금액 WAPE 19.4~28.1%→18.3~18.5%로 갱신(우열 판정 불변) — 2장 버전정합성 갭 중 예측기 항목 해소, 진단기(4-3)만 잔존. |
| v1.2 | 2026-07-24 | 사용자 요청으로 4-2·4-3·4-4 **적대적 감사**(5장 신설, 3개 서브에이전트 병렬+핵심 발견 직접 재검증). 실측 확인 후 코드 수정 3건 즉시 반영: ① `geo/indexer.py` 국가명 정규화(DRC 5종 표기→USGS "Congo (Kinshasa)", Myanmar→Burma — CO 이벤트 44.6%가 조용히 conc=1.0 중립 폴백되던 버그), ② `geo/config/index.yaml` scale_k v2→v3 재앵커(P90 목표 88 복원, `GEO_INDEX_VERSION` v2→v3), ③ `geo/prob_model.py` NB2 MLE 수렴 체크 추가. ④ `msr/models/forecast_unit.py` 환율(fx) 피처 100% 결측 버그 수정(CSV skiprows 오류로 한 번도 실제 반영된 적 없었음). 전부 재실행·재발행으로 검증: geo_index/geo_prob(NB2 Brier 우열판정 불변, ±0.002 이내), forecast_unit(WAPE 22.9%/15.4%로 갱신, 계절나이브 대비 우위·재귀 채택 불변). 5-2·5-3에서 추가 발견한 구조적 한계(진단모델 타깃-피처 순환성 실측 확정, y_lag1 지배 재확인, 외생피처 영향력 미미)는 코드 수정 대신 6장에 후속 항목으로 기록(문서 문안 교정·XAI 안정성 필터 등). |
| v1.3 | 2026-07-24 | 사용자 지시("수정된 것을 기반으로 다시 점수화")로 마지막 남은 버전정합성 갭(4-3 진단기)을 해소 — `mart_weekly_diagnosis` 재빌드(`msr.features.weekly_mart`, geo_index v3 반영)→`nowcast`→`alert` 전체 체인 재실행, `out_diagnosis_alert` 1,632행 갱신(generated_at 07-17→07-24). **결과**: QWK는 0.9687로 전혀 안 바뀜 — 5-2 감사의 "QWK≈y_lag1 복사값" 발견을 데이터로 재확인(지수를 이만큼 고쳤는데도 안 움직이는 것 자체가 그 발견의 증거). 대신 순수 지정학신호만 보는 GEO_ONLY_NO_LAG는 정상 반응(전환주 적중률 챔피언 Ridge(풀링) 0.5385) — 이 지표를 4-3 표에 신규 병기해 "지수 변경이 실제로 어디에 반영되는지"를 명확히 함. 이로써 2장 버전정합성 갭이 완전히 해소돼 5개 모듈 전체가 2026-07-24 기준으로 동일 시점 정합 상태(geo_index/geo_prob 04:36, forecast_unit 04:40, out_diagnosis_alert 15:23 — 전부 오늘). |
| v1.4 | 2026-07-24 | 사용자 지시("y_lag1 의존 문제를 피처 조합·모델 교체로 풀 수 있는지 딥하게 검토")로 5-2 발견 #2의 미착수 대안 6계열을 일괄 백테스트(`scripts/diagnosis_ylag_deep_review.py`): E1 비대칭 상향게이트·E2 Δ타깃 전환분류·E3 서수모델(OrderedModel)·E4 전환가중학습·E5 동역학 피처확장·E6 잔차회귀 + 후속으로 E7 방향별 이벤트 카운트 피처(geo_event.direction 주간 집계)와 확률임계 게이트(변형 D)까지 검정. **전부 기각(일곱 번째 동일 결론)** — 형식 기준 유일 통과였던 E4(w=20)는 강건성 검증(세밀스윕·부트스트랩)에서 뒤집혔고(오경보:적중 ~11:1, QWK CI 하한 허용선 침범), E7의 큰 개선(전환주 적중 0.50)은 ablation으로 가격뉴스 반향(라벨 오염) 판명. 결론: 병목=피처 정보량, 외부 신규 직교 데이터(LME 재고·선물커브 등) 확보 전 재시도 무의미. 5-2 표 #2 대응란 갱신. 학습기간 확장 불가(geo_prob 2016-01 시작) 실측 확인 포함. |
| v1.5 | 2026-07-24 | /goal("현재 데이터 외 필요한 정보 확보→수집→모델링 반영→기대효과 체크") 실행. 발주처 원본 실사에서 미활용 데이터 발굴 — 주간 LME재고(동·니켈 2007~2026)·거시 12종을 `scripts/load_market_aux.py`로 `fact_inventory` 2,030행·`fact_series` 3,373행 적재(비어 있던 두 테이블 최초 가동, 원자재지수는 PRICEIDX_ 접두어로 오염 격리). `scripts/diagnosis_aux_features_eval.py`(as-of 누수방지)로 검정: **재고 피처가 CU·NI Δ분류 프레임에서 전환주 적중 1/18→7/18·QWK 동반상승(0.810→0.839) — 세션 최초의 "적중↑+QWK↑" 동시 성립**, 부트스트랩 95% CI [0.111,0.556]·P=1.000. 거시 피처는 커버리지 교란(2021-06+)으로 보류, 게이트 결합은 여전히 기각(8번째), 운영 등급예측은 지속성 중심 유지 — **기대효과 "부분 충족"**(직교 데이터가 곡선을 이동시킴을 실증, 단 보조신호 개선 수준). 4-3 표에 신규 동작점 병기. 수집 확장: CO=LME 무료공개 스크레이퍼 후보, LI=유료 차단, REE=구조적 불가. |
