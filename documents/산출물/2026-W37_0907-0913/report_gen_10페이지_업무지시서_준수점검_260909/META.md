# META — report_gen 10개 페이지 업무지시서 준수점검 (2026-09-09)

## 무엇인가

사용자 요청: "지금 10개의 보고서를 작성하고 지시서에 있는 내용과의 준수 여부를
체크해주세요. 광물가격은 2024~2026년 데이터를 기반으로 하세요." — report_gen이
제공하는 전체 10개 page_id에 대해 실 KOMIS 데이터로 결과문을 직접 생성하고,
발주처 2026-09-09 업무지시서(`documents/기획문서/order/0909/report_summary_feedback/
AI_통계분석_요약기능_수정_업무지시서.md`)의 §2 공통원칙·§3.1~3.3 메뉴별 지시·§4
품질오류 체크리스트와 항목별로 대조했다. 이후 main-agent의 원천 데이터 재검증
(A/C/B 3라운드)과 사용자의 실 배포 API 재확인을 거쳐 지금 상태로 확정됐다 —
아래 각 절은 그 최종 상태를 담는다(경위는 `00_업무지시서_준수여부_점검.md`의
해당 절 참고).

본문은 `00_업무지시서_준수여부_점검.md` 하나에 담았다(이전 라운드들처럼 페이지별
파일로 쪼개지 않음 — "개선 전/후 비교"가 아니라 "현재 상태 일괄 점검"이라 한 문서로
보는 게 더 실용적이라고 판단). §2.4 산출물 중 ①메뉴별 최종 답변 템플릿·③주요
지표 산출표는 별도 문서(`01_메뉴별_최종_답변_템플릿.md`·`02_주요_지표_산출표.md`)
로 작성했다 — 코드의 실제 f-string·산식에서 직접 역산(임의 서술 없음).

## 데이터 출처

- 광물자원가격 4종(price_*)은 사용자 지시대로 **2024~2026년 데이터**로 필터링했다
  (니켈·가돌리늄은 `request.start_date="2024-01-01"`로 필터, 철·금은 원본 실측
  캡처가 이미 2024-01-02~2026-08-27이라 별도 필터 불필요).
- indicator_composite는 원본 덤프의 5개 기간 콤보(1개월/3개월/6개월/1년/
  전체(2016~)) 중 **"전체(2016~)"**(2016-01-01~2026-08-26)를 쓴다 — "1년"
  콤보는 구조상 전년 대비 비교점을 못 만들어(아래 "정정 이력" 참고) 대표
  예시로 부적합했다.
- 나머지 5종(광물전망지표 2종·수급지도 2종·광물지도 1종)은 발주처가 제공한 실
  KOMIS 덤프(`income_data/komis/komis_04~08_*.json`)를 그대로 `komis_response`
  (또는 `komis_snapshot_response`)로 넘겨 조회 시점 그대로 썼다.

## 결론 요약

- §3.1(광물자원가격) 4종, §3.2(광물전망지표) 3종, §3.3(수급지도·광물지도) 3종
  — 전부 ✅(발주처 템플릿 구조·외부원인 배제·기술지표 미노출·단위 표기·비중
  검증·격식체 통일·섹션 표시명 정합 전부 반영 확인).
- §3.2의 개별 광종 가격(composite)·투자환경지수 하위요인(market)은 요청 JSON
  모델 자체에 없어 계산 불가한 구조적 한계로 남아 있다(사용자가 신규 엔드포인트
  연동을 하지 않기로 확정, `feedback_report_gen_json_only_no_new_endpoints`
  메모리 참고) — 코드 결함이 아니라 설계상 범위 밖. supply의 "구성요소 변화"는
  2026-09-10 4/5 요인(가격리스크·국내수입증가율·국내수입국편중도·세계공급편중도)
  구현으로 이 목록에서 해소됐다 — 마지막 1개(세계수급비율)는 모델 필드가 아니라
  KOMIS 응답 자체가 항상 빈 배열이라 제외.
- §2.4 산출물 3종(①②③) 전부 완료.
- 남은 사용자 결정 대기 항목은 D-10(map_korea/global "기준일" 표기가 KOMIS
  조회 파라미터 echo라 미완결 연도로 조회하면 미래 날짜로 보이는 문제) 하나뿐
  — 완결된 과거 연도로 조회하면 증상 자체는 재현되지 않는다(아래 "실 배포 API
  재검증" 참고), 코드 수정 여부는 사용자 결정 대기.

## 실행 방법(재현 절차)

- **문서에 실은 10개 원문**: `gen_10_reports.py`(세션 스크래치패드) —
  `AnalysisSummaryService(None, llm=None)` 직접 생성 → 10개 page_id별
  `AnalysisSummaryRequest` 조립 → `.analyze()` → `report_render.
  render_markdown_report()`(규칙기반, LLM 미개입 — 결정론적 재현·바이트 대조
  용도). `verify_10_reports_doc.py`로 스크립트 실제 출력과 바이트 단위 대조
  (10/10 일치).
- **실 배포 API 재검증**(2026-09-10, 사용자 요청): 컨테이너
  `komir-report-gen:260910-outlook-fix-v2`(18003 포트)에 HTTP로 직접 호출 —
  2026-08-30부터 라우터 요청 모델이 `start_date`류 사후필터 필드를 없애
  "기간은 komis_response 자체에 담을 행으로 통제"하는 계약으로 바뀌어서,
  price_*는 원본 응답의 날짜 행을 직접 트리밍하고 map_korea/global/indicator_
  market/supply는 덤프에 있는 연도·월 단위 콤보 중 적절한 것을 선택해 보냈다.
  10페이지 전부 200 응답 + 지시서 기준 정상 출력 확인(LLM 정제 결과 일부 포함).

## 검증

- pyflakes 0경고, 395콤보 전수(오류·불일치 0).
- 문서에 실은 10개 결과문 전부 `render_markdown_report()` 실행 결과와 바이트 단위
  일치 확인(`verify_10_reports_doc.py`, 10/10 PASS).
- 금지 표현 재검사(10개 원문 전체) 0건.
- 실 배포 API 응답 10건도 위와 별도로 육안 대조 완료(격식체·섹션명·단위 표기·
  §2.2 인과단정 배제 전부 일치).

## 2026-09-10 후속 — 발주처 3차/2차 피드백 8항목 반영

이 문서 최초 작성(2026-09-09) 다음 날, 발주처(권가영 사원)가 8개 항목
([1]~[8])을 추가 지적했다 — price_* RSI/가격위치 문구, indicator_composite
전주/전월/전년 값+지수별 분리 서술, indicator_market 점수-위험 반비례
문장 분리, indicator_market/supply 단계유지 시작월·"화면상" 문구 제거,
전 페이지 공통 날짜서식(YYYY년 MM월) 통일. 전부 report-summary-agent
워크트리에서 구현·검증(pyflakes·unittest 14종·395콤보 스모크·실 KOMIS
렌더링) → main-agent 독립 재검증 → main 병합·재배포·라이브 curl 확인
사이클로 완결했다 — 상세는 `00_업무지시서_준수여부_점검.md`의 동명 절,
갱신된 템플릿은 `01_메뉴별_최종_답변_템플릿.md` 참고.

같은 배치 도중 main-agent가 배포 인프라 버그(수동 `docker run`에
`LLM_BASE_URL` 컨테이너 오버라이드 누락으로 하루 종일의 라이브 검증이
실제로는 규칙기반 폴백이었던 것 — 2026-09-01에 이미 한 번 겪은 것과
같은 클래스의 재발)와 실LLM 재검증 중 발견한 [5] 문장 삭제 회귀
(`_validate_llm_summary` 타겟 검증으로 수정)를 발견·수정했다.

## 2026-09-10 재재후속 — 10개 결과문을 실LLM 정제 응답으로 교체

바로 위 절의 예시는 규칙기반(`llm=None`) 렌더링이었다. 사용자 요청으로
실제 배포 컨테이너(260910t)의 실LLM 정제 응답으로 전부 교체했다(사실
관계는 동일, 문장 연결만 자연스러워짐) — 10페이지 전부 성공(재시도
1~5회), 상세는 `00_업무지시서_준수여부_점검.md`의 동명 절 참고. 이
과정에서 라이브 라우터 요청 스키마가 로컬 테스트용 스키마와 다르다는
것(`observations`/`start_date` 제거, `komis_response`만 받음 — price_*
2종은 예외적으로 `observations` 하위호환 유지)을 확인했다.

## 2026-09-10 재재재후속 — 지도 3종 단위 간소화+대괄호 제거+전년비교, 수급동향지표 구성요소 상세 강제

발주처 2차 피드백([수급지도]·[글로벌 수급지도]·[광물지도])과 사용자 제보
(수급동향지표 "구성요소 변화" 선문장만 나오고 실제 근거가 안 보이는 문제)를
반영했다. 사용자가 Codex로 작업을 시작했다가 rate limit로 중단, main-agent가
이어받아 검증·완결(커밋 `0cb9f84a0`, 이미지 `260910v`). 신규 `map_presentation.py`
(억/만 단위 축약, 전년 동기 비교), map_korea 1~3위 표출 일관화+수출입 해석
보강, map_global 대괄호 제거, map_mineral 톤 단위 간소화, indicator_supply
선문장 제거를 담았고, `_validate_llm_summary`에 구조적 백스톱 4건을 추가해
"프롬프트 지시만으론 확률적으로 새는" 문제를 코드 레벨에서 재차 방어했다.
상세는 `00_업무지시서_준수여부_점검.md`의 동명 절, 갱신된 예시는 "7~10.
결과문 원문" 참고. map_global의 "참고: 연도별 교역액 변화"(1위 교역국 자체
시계열)는 사용자 확인을 거쳐 그대로 유지하기로 확정됐다.

## 정정 이력

- **map_mineral 단위**: "천톤"→"톤"(USGS 교차대조로 확정, KOMIS 화면 실표기
  재확인은 온라인 가능한 시점의 후속 과제로 남음).
- **광물종합지수 "전년 대비"**: 최초엔 "코드 미구현"으로 오판 → 합성 데이터로
  "코드는 있고 표본이 363일이라 며칠 모자랄 뿐"으로 1차 정정 → 2026-09-10
  사용자 지적으로 재확인한 결과, 진짜 원인은 `komis_dump_smoke_test.py::
  adapt_mineral_index()`가 원본 덤프 5개 콤보 중 구조적으로 전년 대비를 못
  만드는 "1년" 콤보에 고정돼 있었던 것 — 같은 덤프의 "전체(2016~)" 콤보로
  바꾸니 실데이터로 정상 발동(+34.56%), 합성 데이터 우회 자체가 불필요했다.
