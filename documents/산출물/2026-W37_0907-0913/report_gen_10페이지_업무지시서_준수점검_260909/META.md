# META — report_gen 10개 페이지 업무지시서 준수점검 (2026-09-09)

## 무엇인가

사용자 요청: "지금 10개의 보고서를 작성하고 지시서에 있는 내용과의 준수 여부를
체크해주세요. 광물가격은 2024~2026년 데이터를 기반으로 하세요." — report_gen이
제공하는 전체 10개 page_id에 대해 실 KOMIS 데이터로 결과문을 직접 생성하고,
발주처 2026-09-09 업무지시서(`documents/기획문서/order/0909/report_summary_feedback/
AI_통계분석_요약기능_수정_업무지시서.md`)의 §2 공통원칙·§3.1~3.3 메뉴별 지시·§4
품질오류 체크리스트와 항목별로 대조했다.

본문은 `00_업무지시서_준수여부_점검.md` 하나에 담았다(이전 라운드들처럼 페이지별
파일로 쪼개지 않음 — "개선 전/후 비교"가 아니라 "현재 상태 일괄 점검"이라 한 문서로
보는 게 더 실용적이라고 판단).

## 데이터 출처

- 광물자원가격 4종(price_*)은 사용자 지시대로 **2024~2026년 데이터**로 필터링했다
  (니켈·가돌리늄은 `request.start_date="2024-01-01"`로 필터, 철·금은 원본 실측
  캡처가 이미 2024-01-02~2026-08-27이라 별도 필터 불필요).
- 나머지 6종(광물전망지표 3종·수급지도 2종·광물지도 1종)은 발주처가 제공한 실
  KOMIS 덤프(`income_data/komis/komis_03~08_*.json`)를 그대로 `komis_response`
  (또는 `komis_snapshot_response`)로 넘겨 조회 시점 그대로 썼다 — 이 6종은 사용자
  지시가 "광물가격"에 한정돼 있어 기간 필터를 별도로 적용하지 않았다.

## 결론 요약

- §3.1(광물자원가격) 4종, §3.3(수급지도·광물지도) 3종은 전부 ✅(발주처 템플릿
  구조·외부원인 배제·기술지표 미노출·단위 표기·비중 검증 반영 확인).
- §3.2(광물전망지표) 3종은 부분 반영(⚠️) — 개별 광종 가격/하위요인/2개 요인
  데이터가 요청 JSON 모델 자체에 없어 계산 불가한 구조적 한계(사용자가 신규
  엔드포인트 연동을 하지 않기로 이미 확정, `feedback_report_gen_json_only_
  no_new_endpoints` 메모리 참고) — 새로 발견된 문제가 아니라 재확인.
- 이번 점검 중 indicator_supply에 남아있던 인과단정 표현("...분석된다")을 새로
  발견해 즉시 수정·병합(커밋 5b8221f9c, 이 점검 이전에 이미 완료).
- 이전 audit에서 "광물종합지수 전년 대비 누락"이라고 잘못 보고했던 것을 이번에
  합성 데이터로 재검증해 정정(코드는 이미 구현돼 있었고, 표본이 363일이라 1년
  비교가 발동하지 않았을 뿐).

## 실행 방법(재현 절차)

`gen_10_reports.py`(세션 스크래치패드) — `AnalysisSummaryService(None, llm=None)`
직접 생성 → 10개 page_id별 `AnalysisSummaryRequest` 조립 → `.analyze()` →
`report_render.render_markdown_report()`. 문서에 실은 10개 원문은 `verify_10_reports_
doc.py`로 이 스크립트의 실제 출력과 바이트 단위 대조까지 마쳤다(10/10 일치).

## 검증

- pyflakes 0경고, 395콤보 전수(오류·불일치 0) — 둘 다 이번 세션 누적 검증치 재확인.
- 문서에 실은 10개 결과문 전부 `render_markdown_report()` 실행 결과와 바이트 단위
  일치 확인(`verify_10_reports_doc.py`, 10/10 PASS).
- 금지 표현 재검사(10개 원문 전체) 0건.

## 후속 개정(2026-09-09, 같은 날) — main-agent 재검증 반영

main-agent가 이 문서를 USGS 등 원천 데이터와 교차 재검증해 실제 문제 7건(A-1~3·
C-6~9)을 발견 — 커밋 `a775d49b6`으로 전부 수정하고 10개 결과문을 재생성·재검증
했다(다시 `verify_10_reports_doc.py`로 10/10 일치 확인). 상세는 본문의
"main-agent 재검증으로 발견·수정한 항목" 절 참고. 요약:
- map_mineral 단위 "천톤"→"톤" 정정(USGS 대조로 확정, KOMIS 화면 재확인은 후속),
  #10 샘플의 교차비교 데이터가 다년 합산값이었던 것도 재생성.
- map_global 한자·단위표기 2건, price_momentum 보합일수 누락, 비price 표 소숫점
  잔존 — 총 4건의 소규모 코드 버그 수정.
- §2.4 산출물 3종 중 ①메뉴별 최종 답변 템플릿·③주요 지표 산출표는 아직 미작성
  (본문에 별도 절로 명시) — 후속 작업 필요.

## 후속 개정 2 (2026-09-09, 같은 날) — B-3·B-5·B-4 반영

main-agent가 지시서 대비 잔여 격차 3건(B)을 추가 지적, 사용자 승인 하에 전부
반영했다(커밋 `cf80bc720`·`1c95fbd86`·`f5a459f84`, main 병합 `ec2ceac70`·
`d835f0f44`). 상세는 본문의 "B(지시서 대비 잔여 격차) 반영" 절 참고. 요약:
- B-3: 10개 page_id 전부 격식체(~습니다) 통일(편집 가능 4종 직접 전환 + frozen
  2종 post-processing 계층 신설, 매핑 템플릿 28개·미매핑 경고 0건).
- B-5: 6종 페이지의 `##` 섹션 표시명을 업무지시서 블록명에 가깝게 확정(map_mineral은
  measure별 동적 타이틀).
- B-4: §2.4 산출물 2종(①메뉴별 최종 답변 템플릿·③주요 지표 산출표) 작성 완료 —
  `01_메뉴별_최종_답변_템플릿.md`·`02_주요_지표_산출표.md`(이 폴더), 코드의 실제
  f-string·산식에서 직접 역산해 작성(임의 서술 없음).

10개 결과문은 이 배치까지 전부 반영한 최신 버전으로 재생성·바이트 대조
완료(10/10 일치).
