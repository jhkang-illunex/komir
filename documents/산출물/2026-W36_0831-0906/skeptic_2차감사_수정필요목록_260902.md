# skeptic-code 2차 DEEP 감사 — 수정 필요 목록 (2026-09-02)

> 사용자 요청: "금일 이전까지 수정이 있었던 chatbot, summary-report, 프롬프트 관리
> 도구에 대해 딥하게 코드 리뷰 — 지금 당장 수정하지 말고 수정해야 할 내역만 리스트업."
> **report-only 실행** — 이 문서 작성 시점에 코드는 한 줄도 수정하지 않았다.
>
> 감사 범위: 지난 1차 DEEP 감사(커밋 `575979a52`, 2026-09-01) 이후의 미감사
> 변경분(`575979a52..62e8af91f`, 챗봇 +135줄 / report_gen +746줄) + 프롬프트 관리
> 체인은 diff가 아니라 **체인 전체**(prompt_admin.py·prompt_store.py·seed_prompts.py·
> /admin/prompts/reload·resolve_page_config)를 처음으로 단위 감사.
> 방식: 서비스별 병렬 fork 3개, Pass 1A(존재성 12항목)+1B(안전성 6항목) 전수,
> HIGH/MEDIUM 후보는 전부 재현 시도(총 8건 재현 성공 — 실컨테이너 stdio MCP 호출·
> 몽키패치·in-memory 입력, 워킹트리 무변경).

## 우선순위 종합 (수정 필요 13건 + 결정 필요 3건)

| 순위 | ID | 도메인 | 심각도 | 한 줄 요약 | 재현 |
|---|---|---|---|---|---|
| 1 | PA-002 | 프롬프트관리 | **HIGH(운영사고 소지)** | seed 재실행이 운영자 편집을 전 컬럼 무경고 덮어쓰기 — 배포 절차는 seed 재실행을 의무화, UI 경고·백업 없음 | DB 실측 |
| 2 | PA-001 | 프롬프트관리 | MEDIUM | reload 실패가 `{"reloaded_prompt_count": N}` 성공 응답으로 위장 — 편집 미반영을 운영자가 알 수 없음 | ✅ |
| 3 | SC-R2-001 | report_gen | MEDIUM | 수급동향 snapshot 경로: 파서 자신이 만든 FP 오차(100.00000000000001)가 `le=100` 위반 → 페이지 전체 INTERNAL_ERROR | ✅ |
| 4 | SC-R2-002 | report_gen | MEDIUM | 가중치 YAML 로드 실패 시 광물종합지수 페이지 전체 사망(폴백 없음) — 패키징 누락 한 번이면 배포 직후 전 요청 실패 | ✅ |
| 5 | SC-CB2-001 | chatbot | MEDIUM | 광종 미지정 composite 조회는 더미데이터 강제경고(caveat)가 통째로 빠짐 — fail-open | ✅ |
| 6 | SC-CB2-002 | chatbot | MEDIUM | 광종 지정 composite 조회는 광종 무관 지수를 "(니켈)"처럼 그 광종 데이터로 오표기 | ✅ |
| 7 | SC-R2-003 | report_gen | MEDIUM | 비연속 관측치에서 4개월 전 값을 "전월"로 오표기 — 인용강제 설계상 최종 보고서까지 전파 | ✅ |
| 8 | PA-003 | 프롬프트관리 | MEDIUM | 스키마 틀린 output_contract(키 오타 등)가 저장·reload "성공" 통과 후 런타임에서 조용히 무시(로그도 없음) | ✅ |
| 9 | PA-004 | 프롬프트관리 | MEDIUM | 기능테스트 광종 드롭다운이 report_demo의 registry 확장(39/36/73/34/3종)을 못 따라감 — TWIN 드리프트 3차 실현 | grep 대조 |
| 10 | SC-CB2-003 | chatbot | MEDIUM | smoke test의 private 통과 assert가 or-조건이라 private 경로가 조용히 죽어도 통과 — 게이팅 회귀망 절반이 빈 상태 | ✅ |
| 11 | SC-R2-005 | report_gen | LOW | 5단계 전환으로 도달 불가능해진 grade-None 분기가 폐기된 4단계 정책 문구("0~1점 미확정")를 물고 있음 | 코드 추적 |
| 12 | PA-005 | 프롬프트관리 | LOW | UPDATE 0행이어도 "저장 완료" — execute_pg가 rowcount를 안 돌려줌 | 코드 추적 |
| 13 | PA-008 | 프롬프트관리 | LOW | schema_ai_cfg.sql 주석이 구식(5개 키 시절) — 현재 13키 시딩과 불일치 | DB 실측 |

**결정 필요(QUESTION) 3건** — 실측/의도 확인 후 수정 여부 결정:

| ID | 도메인 | 질문 |
|---|---|---|
| SC-R2-004 | report_gen | subChart03 파이가 3개국 이하로 오는 경우가 실제로 있는가? 있으면 "상위 3개국 의존도 100%"가 계산 방식의 항등식인데 실측처럼 서술됨 — claim 생략 또는 "나열국 소계 기준" 한정어 필요 |
| SC-R2-006 | report_gen | 시장동향 crtrYmd(8자리)가 월 1행 보장인가? 월중 재게시가 오면 동월 스냅샷 2건을 "전월 대비"로 비교하게 됨 — dedup 1줄 필요 여부 |
| PA-007 | 프롬프트관리 | summary_common 선택 시 페이지 정책 편집창이 뜨지만 저장해도 어떤 경로에서도 안 읽힘(침묵 no-op) — expander를 숨길지, 향후 확장 여지로 남길지 |

---

## 상세 — chatbot (FIX 3건)

### SC-CB2-001 [LIAR·MEDIUM] 광종 미지정 composite 조회의 더미경고 소실
- 위치: `inhouse/rag/ragkit/_mcp_tools_common.py` is_dummy 결정부 + `chatbot_graph.py:424` composite 분기
- 재현: private 프로필에서 `call_komis_raw_lookup('indicator_composite', mineral_code='MNRL0002')`는 caveat+경고가 붙는데, **같은 테이블 같은 행**을 mineral_code 없이 조회하면 caveat=None, warnings=[] — 경고 완전 소실.
- 원인: is_dummy 판정이 `if mineral_code:` 안에만 있어(resolve_mineral_meta가 광종 키 기반) 광종 미지정 경로는 판정 자체를 건너뛰고 **fail-open**. "광물종합지수 알려줘"는 d399ef614로 명시적으로 열어준 실도달 경로다.
- 처방: 광종 필터 없는 page는 더미 판정 기준을 별도 확보하거나, 판정 불가 시 fail-safe(caveat 부착) 기본값으로. KO_MNRL_SNTHS_INDX의 실제 출처(실데이터/더미) 확인 선행 필요.

### SC-CB2-002 [LIAR·MEDIUM] 광종 지정 composite 조회의 오표기
- 위치: `chatbot_graph.py:425-428` + `_mcp_tools_common.py` mineral_label suffix
- 재현: '니켈 광물종합지수' 경로 → `Evidence.section = "KOMIS 원천 · KO_MNRL_SNTHS_INDX(니켈)"`. 그러나 indicator_composite의 `_DatasetSpec`은 mineral_code 필터가 없어(스펙에 없는 필터는 조용히 무시) 실제 결과는 **전체** 광물종합지수 — 니켈 필터가 아니다.
- 위험: 2026-09-01 텅스텐 section 버그와 같은 계열 — 생성 LLM이 종합지수를 "니켈의 지수"로 서술할 여지. 더미판정도 니켈 행 기준이라 무관한 판정.
- 처방(권장): composite 분기에서 komis_raw_lookup에 mineral_code를 아예 안 넘김(조회에 효과가 없으므로). 또는 광종 필터 없는 page_id는 suffix·더미판정에 mineral_code 미사용 가드.

### SC-CB2-003 [LIAR·MEDIUM] smoke test private assert 무력
- 위치: `inhouse/services/rag_chat/tests/smoke_mcp_access.py:106`
- `assert pri_ev != [] or not any("private 전용" in w for w in pri_warn)` — private 경로가 조용히 죽어 evidence 0건·경고 없음이어도 통과. 실제로 잡는 건 "private 서버에 실수로 게이팅이 넘어간 경우" 하나뿐 — 주석("정상 조회돼야 한다")과 검증 강도 불일치.
- 처방: `assert pri_ev != [], (page_id, pri_warn)`로 강화(세 테이블 전부 현재 데이터 보유 확인됨 — 빈 테이블 허용 완화는 그때 가서).

**chatbot 오탐 기각**: PRIVATE_ONLY_KOMIS_PAGES 소스리터럴 전달(확정 신뢰경계 설계), composite 분기 하드코딩·resolve 3줄 중복(KISS 정당). 게이팅 우회 경로 없음 확인(테이블 접근자는 komis_raw_lookup 유일 + pydantic Literal 재검증). ROUTE_PROMPT 회귀는 병합 시점 라이브 5케이스로 기검증.

---

## 상세 — report_gen / summary-report (FIX 2·ADD 1·CUT 1·QUESTION 2)

### SC-R2-001 [LIAR·MEDIUM] snapshot 경로의 검증 우회 + FP 오차 자폭
- 위치: `summary.py:1871-1874` + `_parse_komis_supply_snapshot_response`의 share 계산
- 재현: subChart03에 특정 3개국 값 조합 → `top_three_dependency_percent=100.00000000000001`(부동소수 합산 오차) → `Field(le=100)` 위반 → ValidationError → **INTERNAL_ERROR**(전체 요청 사망). 기존 손매핑 경로는 R2-F1 래핑(→NO_DATA)이 있는데 신설 snapshot 경로만 맨몸 model_validate.
- 처방: ① 합산에 `min(100.0, ...)` clamp ② `_supply_auxiliary_from_request`와 동일한 DataSourceError 래핑. 둘 다 필요(clamp만 하면 음수값 케이스, 래핑만 하면 정상 데이터 NO_DATA).

### SC-R2-002 [ORACLE·MEDIUM] 가중치 YAML 로드 실패 = 페이지 전체 사망
- 위치: `additional_summary.py:178-192` `_load_mineral_index_weights`
- 재현: 파일 부재 시 FileNotFoundError 그대로 전파 → INTERNAL_ERROR. yaml 오타·키 누락도 동일. lru_cache는 예외를 캐시하지 않아 매 요청 실패.
- 처방: try/except → 빈 dict 반환 + `_log.exception`. `if composite_weights:` 가드가 이미 있어 가중치 절만 조용히 빠지고 페이지는 정상 렌더링(추가 분기 불필요).

### SC-R2-003 [LIAR·MEDIUM] 비연속 월의 "전월" 오표기
- 위치: `summary.py:1272-1277`(53425f1e4의 퍼센트 서술)
- 재현: 2026-03→2026-07 비연속 관측치 → `"직전 관측치 대비에는 전월 45.00점 대비 11.11% 상승하며…"` — 라벨과 본문 모순, 45.00은 4개월 전 값. KOMIS 결측월 있으면 실도달.
- 처방: `previous_label = "전월" if months_are_contiguous(...) else f"직전 관측치({previous.month})"`.

### SC-R2-004 [ORACLE·MEDIUM·QUESTION] 파이 ≤3개국이면 의존도 100% 항등식
- share 분모가 세계 총액이 아니라 나열국 소계(의도된 설계)라, 나열국이 딱 3개면 top3 합은 **항상** 100% — "집중된 구조다"가 측정이 아니라 계산 방식의 결과. 실측 덤프의 국가 수 확인 후: 항상 4개국+면 닫고, 3개 이하 가능하면 claim 생략 또는 한정어.

### SC-R2-005 [GHOST·LOW·CUT] 도달 불가 grade-None 분기 + 폐기 정책 문구
- 위치: `summary.py:1234-1241, 1330-1333, 1345-1352`
- 036eb231e 이후 `policy.classify()`는 None을 반환하지 않고(실패 시 PolicyError), 새 5단계가 0~100 전 구간을 덮어 None 유입 경로 없음. 그런데 죽은 분기가 "0~1점 구간은 단계를 확정하지 않는다"는 **폐기된 4단계 문구**를 물고 있음 — 되살아나면 새 정책과 정면 모순.
- 처방: 분기 제거(+타입 `list[GradeResult]`로 좁히기), 또는 최소수정으로 구정책 문구만 교체.

### SC-R2-006 [ORACLE·LOW·QUESTION] 동월 중복 스냅샷 dedup
- 재현: crtrYmd 20260801/20260815 → month ['2026-08','2026-08'] 중복 → 같은 달 두 스냅샷을 "전월 대비"로 비교. KOMIS 월중 재게시가 실제로 있는지 확인 후 dedup(최신 우선) 1줄 여부 결정.

**report_gen 오탐 기각**: 빈 komis_response→침묵 폴백 없음(명시적 NO_DATA 확인), `_to_polite_copula` 함정 전수 스윕 — 12페이지 정의문 전부 실함수 통과 확인(비문 0건), previous.score=0 가드 존재, major_changes 용량 정합, 가중치 detailed_metrics 배치·subChart04/05/07 미파싱·5단계 구간값·weight_kg 선택화(전부 확정 설계).

---

## 상세 — 프롬프트 관리 체인 (FIX 4·ADD 1·QUESTION 1)

### PA-002 [CLIFF·HIGH(운영사고 소지)] seed 재실행의 무경고 편집 소멸
- 위치: `seed_prompts.py:43-57` `_UPSERT_SQL` + prompt_admin.py 전체(경고 부재)
- SQL 정독: ON CONFLICT DO UPDATE가 content·page_definition·output_contract 등 **전 컬럼**을 코드 기본값으로 조건 없이 덮음. DB 실측: 13행 전부 updated_at이 마지막 seed 시각으로 통일 — 그날 UI 편집이 있었다면 전부 소실됐을 상태. 배포 절차는 seed 재실행을 **의무화**하고 있고(2회 실사고로 규칙화), UI엔 "배포 시 초기화될 수 있음" 표시가 전혀 없음.
- 처방(최소 하나, 둘이면 더 좋음): (a) seed가 덮어쓰기 전 현재 행 백업(가장 저렴) (b) prompt_admin에 필드별 "코드 기본값과 다름" 표시 + 경고 캡션 — 서버의 `PageConfig.source("db"|"code")` 맵이 정확히 이 용도인데 UI가 안 씀 (c) `--preserve-edited` 모드.

### PA-001 [LIAR·MEDIUM] reload 실패의 성공 위장 (재현됨)
- 위치: `prompt_store.py:176-192` + `main.py` `/admin/prompts/reload` + `prompt_admin.py:262-269`
- 재현: `_fetch_all`을 강제 실패시켜도 reload()가 예외 없이 이전 캐시 크기(13)를 반환 → 엔드포인트가 `{"reloaded_prompt_count": 13}` — 성공과 바이트 단위 구분 불가. "저장(DB 성공)→reload 중 DB 순단→UI '완료'→실제 보고서는 옛 프롬프트" 연쇄. prompt_admin이 일부러 만든 실패 분기 UI(272행)가 절대 발동 못 함.
- 처방: reload()에 성공 여부 플래그, 엔드포인트는 실패 시 `{"ok": false}` 또는 503 — UI 분기는 이미 있어 서버만 고치면 됨. (기동 시 예외 삼킴 자체는 의도된 회복력 설계 — 관리 엔드포인트의 재사용만 결함.)

### PA-003 [LIAR·MEDIUM] 깨진 output_contract의 침묵 통과 (재현됨)
- 저장은 json.loads 문법만 검증. `section_sentence_range`(s 오타)·`max_evidence_ids_per_sentence: 99`(상한 초과)를 저장하면 → reload "성공" → 런타임 `_parse_output_contract`가 조용히 code 폴백(오타 키는 **로그조차 없음**). 운영자는 계약을 바꿨다고 믿지만 무변화.
- 처방: 저장 전 검증 미러링(간단) 또는 — 권장 — report_gen에 유효 설정 조회 엔드포인트(source 맵 노출)를 두고 prompt_admin이 "실제 db 인식 필드"를 표시. **PA-002(b)와 같은 인프라 공유.** 인식 못 한 키 서버 경고 로그 추가 포함.

### PA-004 [TWIN·MEDIUM] 기능테스트 광종 드롭다운 드리프트 3차 실현
- prompt_admin.py:313, 379가 여전히 평면 `mineral_options()`(19종) — report_demo는 그새 registry 5개 page(39/36/73/34/3종)+price 필터로 확장. 결과: 갈륨 테스트 불가, 잘못된 광종 조합 호출 가능. SC-101(1차)·이번(3차)까지 같은 복제 구조에서 반복.
- 처방(단기): report_demo와 동일 분기 이식. (근본): 광종 위젯 공용 헬퍼 추출 — 기지의 ~130줄 TWIN 리팩터 후속과제의 우선순위 상향 근거. `_PERIOD_PLACEHOLDERS`·기간 int 가드 인라인 중복도 같은 리팩터에 포함.

### PA-005 [ORACLE·LOW] UPDATE 0행 미감지
- `execute_pg`가 rowcount 미반환(shared/db.py:145) — 편집 화면을 열어둔 사이 행이 삭제돼도 "저장 완료". 발생 창이 좁아 LOW. 처방: execute_pg에 rowcount 반환(기존 호출부는 무시라 무해) + 0행 시 오류 표시.

### PA-008 [GHOST·LOW] schema_ai_cfg.sql 구식 주석
- 주석은 5개 page_id 시절 기준("price/map_korea/map_global 제외")이나 현재 13키 전부 시딩(DB 실측 일치). 주석 한 단락 현행화.

### PA-007 [QUESTION·LOW] summary_common의 무효 정책 편집창
- summary_common 행의 page_definition/output_contract는 어떤 경로에서도 안 읽힘(시딩도 NULL)인데 UI는 편집·저장 허용 — 침묵 no-op. 숨길지(저렴), 확장 여지로 남길지 결정 필요.

**프롬프트관리 오탐 기각**: DB 직접 UPDATE 저장 경로(문서화된 확정 계약), 기동 시 예외 삼킴(의도된 회복력), seed 재실행 필요 함정 자체(기지 사항 — 노출 부재만 신규 보고), 캐시 교체 동시성(원자적 재바인딩 설계).

---

## 처방 묶음 제안 (수정 착수 시 참고)

- **묶음 A(프롬프트 관측성)**: PA-001+PA-002(b)+PA-003(b)는 "report_gen이 유효 설정·source 맵·reload 성패를 노출하고 prompt_admin이 표시"라는 **하나의 인프라**로 함께 해결 가능.
- **묶음 B(composite 광종-무관 정합)**: SC-CB2-001+002는 한 뿌리(d399ef614가 조회는 열었지만 광종 키 전제의 라벨·더미판정 메커니즘은 미조정) — 함께 수정.
- **묶음 C(TWIN 리팩터)**: PA-004는 기지의 report_demo/prompt_admin 폼 공용화 후속과제에 흡수 — 3차 재발로 우선순위 상향.
- 단독 수정: SC-R2-001/002/003(각각 독립, 전부 재현된 런타임 결함 — 가장 기계적으로 처리 가능), SC-CB2-003, SC-R2-005, PA-005, PA-008.
- 착수 전 확인: SC-R2-004(실측 덤프 국가 수), SC-R2-006(월 1행 보장), PA-007(의도), SC-CB2-001(KO_MNRL_SNTHS_INDX 실제 출처).
