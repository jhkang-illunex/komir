# 광산자료 집계질의 실시간 계산 파이프라인 — 검증 결과 (2026-09-17)

> PRD: `광산자료_집계질의_실시간계산파이프라인_PRD_260917.md`. 이 문서는 §5 검증
> 계획의 실행 결과다. 컨테이너: `komir-rag-chat:260917-mineagg9`(`komir-rag-chat-test`,
> 18002). 값 검증은 `aggregate_mine_metric()`이 만든 근거표의 "출처" 컬럼이 가리키는
> 원본 OKF `.md`에서 grep 대조하는 스크립트로 수행(원시 로그: 세션 스크래치패드,
> 저장소 미포함 — 아래 표로 대체).

## 0. 데이터 현황 재확인(실측, 문서 §2와 차이 있음)

`ls okf_documents/광산자료/*/`로 직접 확인한 현재 실제 문서 수:

| 광종 | PRD §2 표기 | 실측(현재) | 비고 |
|---|---|---|---|
| 동(구리) | 21 | **20** | |
| 니켈 | 27 | **23** | |
| 코발트 | 20 | **5** | |
| 리튬 | 29 | **28** | |
| 우라늄 | 40 | **11** | |
| 철광석 | 17 | **12** | |
| 합계 | 154 | **99** | |

PRD §2의 154건은 `nas_document/학습데이터/` 원본 파일 수(ingest 이전)다. 2026-09-16
ingest 작업에서 내용 sha256 동일 중복 55건을 제외해 문서-OKF는 99건만 만들어졌다
(`학습데이터_광산자료_중복문서_목록_260916.md`, 메모리 `ingest_layout_...` 참고) —
**이 파이프라인이 조회하는 실제 모집단은 99건**이다. PRD의 154 표기는 그 시점 기록
그대로이며, 이번 구현·검증은 실측치(99건) 기준으로 진행했다.

## 1. 단위 테스트 (§5-1)

`inhouse/rag_chat/tests/test_mine_aggregate.py`(신규, 38건) — LLM/파일 없이 순수
함수만 검사:

- 단위 정규화(`normalize_unit_to_tonnes`): t/kt/Mt/wmt/dmt/Mlb/klb 변환 계수가 PRD
  §4.4-1 표와 정확히 일치, koz·소문자 "mt"(백만톤/미터톤 혼동)는 `None`(비교 제외).
  라이브 검증 중 발견한 회귀도 회귀 테스트로 고정: 대소문자("KTON")·재무제표 관행
  표기("'000 tonnes")도 kt와 동등하게 처리.
- 집계 정렬: max/min/rank(상위5 기본)/compare(대상 불일치 시 "찾지 못함" 기록).
- basis 분리: ore 항목이 metal 순위에서 실제로 제외되는지(Weda Bay 실측 재현).
- 연도 규칙 ①②③ 전부: 명시 연도 필터, 연도 없음 제외, 광산별 최신연도(회계연도
  불일치), 지정 연도가 전무할 때 최신으로 대체(`year_substituted`).
- `found=false`/값 없음 항목 제외.
- 동일 광산 중복 관측치(같은 연도·basis) 처리 — 라이브 검증 중 발견한 회귀를 반영해
  더 큰 값(완전판으로 추정)을 결정적으로 우선하는 규칙을 추가·테스트.
- 회사 전체 합계·불명 광산명("BHP Group"·"unknown") 코드 레벨 배제.

`python3 -m unittest discover -s rag_chat/tests` — **38/38 통과**(기존 35건 포함,
전체 회귀 없음).

## 2. 회귀 (§5-2) — 전부 통과

| 질문 | 기대 동작 | 결과 |
|---|---|---|
| "니켈 1위 생산국은?" | 국가 단위 → pageindex agentic(mine_aggregate 아님) | ✅ tools=[dense,pageindex], mine_aggregate 미호출, 인도네시아 1위로 정상 응답(11.3초) |
| "Kazatomprom 우라늄 광산 위치 알려줘" | 단일 광산 정보조회 → dense(xlsx), mine_aggregate 아님 | ⚠️→✅ **최초 라우팅 회귀 발견**(아래 §4) 후 ROUTE_PROMPT 경계 예시 추가로 수정 확인(9.4초, 좌표 4건 정답) |
| "오늘 서울 날씨 어때?" | off_topic pre-gate, 근거·표 없음 | ✅ 0.4초, 근거 0건 |
| "니켈 가격 알려줘" | komis_raw 표+더미경고, mine_aggregate 무관 | ✅ 7.7초, 표+더미 캐비어트 정상 |

## 3. 목표 질문 재현 (§5-3, PRD §1 원 재현 질문)

**"구리 채굴 광산중 작년에 채굴량이 가장 많은 광산이 어디야?"**

- 이전(구현 전): `no_data_for_period`/`ambiguous`로 잘못 기권(PRD §1 실측 그대로 재현
  확인 — 구현 착수 전 1회 재현함).
- 이후: 20건 중 문서를 전량 확인(SSE `detail` 1/20~20/20 정상 노출) → **최종 응답
  "2025년 기준 구리 채굴량이 가장 많은 광산은 Escondida(BHP)입니다"**(1,305,000t,
  metal 기준) — `Cu_Escondida_Pampa_Norte_BHP.md` 415~419행 "### Escondida" /
  "### 1,305 kt 16% ..." 원문과 grep 대조 일치. 응답시간 41~56초(재현 3회, 아래 §6).
- 인용 근거(`kind="aggregated"`)가 유일 근거로 남아 `_finalize_node`의 노이즈
  가지치기가 정상 동작함을 확인(§4 회귀 참고).

## 4. 구현 중 발견·수정한 버그 4건(라이브로 실제 재현)

1. **route LLM 출력 절단** — `RetrievalRoute`에 필드 5개 추가 후 `max_tokens=220`이
   부족해 JSON이 중간에 잘려 route가 통째로 안전 폴백(dense+pageindex만)으로 떨어짐
   → mine_aggregate가 한 번도 안 켜짐. `max_tokens=350`으로 재확보.
2. **광종 혼입** — Glencore/Anglo American류 복합기업 연차보고서의 "전 사업부문
   요약표"를 그대로 추출 LLM에 주니 철광석(Kumba·Sishen)·다이아몬드(Jwaneng·Orapa)
   수치가 구리 광산인 것처럼 섞여 들어옴 → 추출 프롬프트에 요청 광종(payload.mineral)
   외 항목 배제 규칙 추가.
3. **회사 전체 합계·불명 광산명 오인용** — "BHP Group"(그룹 전체 합계)·"unknown"
   (광산 특정 실패)이 개별 광산처럼 순위에 섞임 → 프롬프트 규칙 추가 + 코드 레벨
   배제(`_is_generic_mine_name`, 프롬프트만으론 100% 안 막혀 이중 방어).
4. **단위 인식 누락** — 대소문자("KTON")·재무제표 관행("'000 tonnes") 미인식으로
   세계 최대급 구리광산(Escondida·Chuquicamata·El Teniente 등)이 전부 "단위 환산
   불가"로 제외됨 → 정규식 대소문자 무시 + 천단위 표기 전처리로 수정.
5. **노이즈 전체기권** — mine_aggregate 근거(1건, 정답 포함)에 안전망 dense 검색
   결과(무관한 조달청 가격동향 보고서 5건)가 함께 붙으면 생성 LLM이 통째로
   ABSTAIN_TEXT를 냄 → `_finalize_node`의 기존 "structured 근거만 남기고 노이즈
   가지치기" 로직 대상에 `"aggregated"`도 포함(§4.2에서 이미 존재하던 komis_raw
   노이즈 버그와 동일 메커니즘·동일 해법).
6. **라우팅 경계 침범(회귀)** — mine_aggregate 도입 직후 "Kazatomprom 우라늄 광산
   위치 알려줘"(단순 정보조회, 비교·순위 아님)까지 mine_aggregate로 잘못 라우팅돼
   기존 xlsx 조회 회귀가 깨짐 → ROUTE_PROMPT에 "비교·순위가 아닌 단일 광산 정보
   조회는 mine_aggregate가 아니다" 경계 예시를 명시적으로 추가해 수정 확인.

## 5. 다중 케이스 값 검증 (§5-6, 필수)

`aggregate_mine_metric()`을 컨테이너 안에서 직접 호출 + grep 대조 스크립트로 검증
(표의 "출처" 컬럼이 가리키는 원본 `.md`에서 "원문 표기" 값이 실제로 존재하는지).
전 케이스 값 컬럼은 실제 원문에서 grep으로 확인됨(✅) — 수치를 지어낸 사례는
발견되지 않았다.

| 케이스 | 결과 | grep 대조 | 비고 |
|---|---|---|---|
| 동: 작년(2025) 생산량 최대(재현 질문) | El Teniente 310,100t 또는 Escondida 1,305,000t/381,700t(2회 반복, 아래 §6 참고) | ✅ 매회 | **값이 실행마다 흔들림**(§6) |
| 동: 상위 3개 → 실제 5개까지 표시(rank 기본 top5) | El Teniente 310,100 / Radomiro Tomic 295,300 / KCC·Mutanda 247,800 / Chuquicamata 245,500 / Sentinel 189,000 (t) | ✅ 전부 | rank 로직 정상 |
| 동: 광석 채굴량(ore) 기준 — basis 전환 검증 | Aktogay 81,024,000t(ore) — metal 기준 결과(El Teniente 등)와 **다른 답** | ✅ | basis 분기가 실제로 다른 결과를 냄을 확인(PRD 요구사항 직접 검증) |
| 니켈: 생산량 최대 | Integrated Nickel Operations(Sudbury·Raglan·Nikkelverk) 39,800t | ✅ | Weda Bay(ore, 41.9 Mwmt)는 basis 다름으로 제외 목록에 정상 표기 — **basis 규칙 직접 검증 성공**(PRD 명시 요구) |
| 리튬: 매장량 최대 — 지표 확장 검증 | Galaxy(Salar del Hombre Muerto 계열) 74,000,000t | ✅ | 코드 추가 없이 파라미터(metric="매장량")만 바꿔 동작 — "범용 템플릿" 목표 달성 확인 |
| 우라늄: 생산량 상위5(문서 11건, PRD 표기 40건은 §0 참고) | "11건 중 7건 확인, 수치 없음"(basis 전부 payable/unknown/환산불가로 metal 기준에서 제외) | N/A | **한계 발견**(§7) — 우라늄은 U3O8 기준 보고가 대부분이라 ore/metal/concentrate/payable 틀이 잘 안 맞음. 응답 59.6~59.8초(문서 11건 기준, 40건이면 비례 증가 예상) |
| 코발트: 두 광산(Mutanda·Kamoto) 비교(문서 5건, PRD 표기 20건은 §0 참고) | "5건 중 2건 확인, 수치 없음", Mutanda는 payable 기준이라 제외·Kamoto는 매칭 실패 | N/A | 코퍼스 자체가 5건뿐이라 비교 대상 데이터 희박(코드 결함 아님, §0 데이터 현황 차이가 원인) |
| 철광석: 생산량 최대(문서 12건, PRD 표기 17건은 §0 참고) | "12건 중 3건 확인, 수치 없음"(ore/payable 기준이 대부분) | N/A | 우라늄과 같은 한계 |
| 연도 지정: "2024년 구리 생산량 최대" | El Teniente 356,400t(2024) | ✅ | 연도 규칙① 정상(2025용 값과 다른 걸 확인 — 310,100 vs 356,400) |
| 연도 미지정 | Escondida 381,700t 또는 Kamoa-Kakula 540,000t(재현마다 다름) | ✅ | 연도 규칙② 자체는 정상(광산별 최신연도 사용) — 값 흔들림은 §6 참고 |
| 값이 없는 지표: "구리 지분율이 가장 높은 곳" | "20건 중 9건 확인, 지분율 수치가 명시된 광산을 찾지 못했습니다"(기권 아님) | N/A | **PRD 요구사항 정확히 충족** — abstain_reason 없이 "확인함, 수치 없음" 응답 |

## 6. 값 흔들림 실측(PRD §5-6 "2회 실행" 요구, 필수 기록)

가장 중요하게 흔들린 케이스: **"동 생산량 최대"(연도 지정 2025, 비지정 둘 다)**.
3~4회 반복 재현 결과, 1위가 아래 세 값 중 하나로 갈렸다(전부 grep 대조상 실재하는
숫자 — 지어낸 값은 없음):

| 회차 | 1위 | 값 | 출처 |
|---|---|---|---|
| 1 | El Teniente | 310,100t | Codelco 자체 보고서(완전판, 100% 기준) |
| 2 | Escondida | 1,305,000t | BHP 자체 보고서(완전판, 100% 기준) — **가장 정확한 값** |
| 3 | Escondida | 381,700t | Rio Tinto 보고서의 **30% 지분 몫**(381.7/0.3≈1,272천t로 역산하면 BHP 수치와 정합) |

**원인**: `Cu_Escondida_Pampa_Norte_BHP.md`는 PDF→마크다운 변환 과정에서 실제
수치("1,305 kt 16% ...")가 그 자체로 별도 헤딩이 돼(415~419행, "### Escondida" 다음
줄이 "### 1,305 kt ..."), 절 제목에 "production"류 키워드가 없어(제목이 광산명
"Escondida" 하나뿐) 지표 키워드 기반 절 매칭(§4.4-6 ①)이 이 절을 후보로 못 잡고
근처의 서술형 절("Record copper and iron ore production", 실제 수치 없이 "16%
증가했다"는 문장만 있음)을 대신 골라 그 문서에서는 값 추출에 실패하는 경우가
있었다(회차 1·3). 그 경우 Escondida를 언급한 **다른 회사(Rio Tinto)의 지분법 수치**
가 유일한 후보로 남아(회차 3) 채택되거나, 아예 다른 광산(El Teniente)이 1위가 됐다
(회차 1). 이번 구현에서 이미 고친 관련 버그 2건(§4-3·§4-4)과 달리 이 문제는 **PDF
변환이 만든 이 문서 특유의 구조적 함정**이라 절 매칭 로직을 상당히 넓혀야
해결되며, 이번 PRD 범위(3단 폴백 §4.4-6)로는 완전히 못 막았다 — §7에 후속 과제로
남긴다. 회차 1·2·3 **모두 fabrication은 아니다**(전부 실제 원문 수치, grep 대조
확인) — 다만 "어느 광산의 어느 수치가 뽑히느냐"가 흔들려 1위 판정이 달라질 수
있다는 한계다. `동 생산량 상위3(rank)`·`동 광석기준(ore)`·`니켈`·`리튬`·`연도지정
2024`는 반복 재현에서 값이 흔들리지 않았다(2회 이상 동일).

## 7. 리스크·오픈 이슈(후속 검토 필요, 이번 범위에서 미해결)

1. **§6 값 흔들림** — Escondida류(수치가 헤딩으로 분리된 PDF 변환 문서)에서 절
   매칭이 실패할 수 있다. 후속 개선 후보: 절 제목 매칭 실패 시 tier-2(키워드 줄
   창)를 tier-1과 **병행**해 후보를 늘리고 숫자 밀도로 재선별하는 방식을 검토했으나,
   초기 언급(회사 전체 합계 문장)이 더 앞줄에 있어 그 방식도 "BHP Group 2Mt"류
   오채택을 다시 부를 수 있음을 확인해 이번엔 보류했다(과잉엔지니어링 위험 —
   사용자 판단 필요).
2. **basis 프레임이 광종마다 안 맞음** — ore/metal/concentrate/payable 4분류는
   구리에는 잘 맞지만 우라늄(U3O8 기준)·철광석(ore 기준 보고가 표준)·코발트(payable
   비중 높음)에는 과도하게 배타적이라 "수치 없음"으로 빠지는 비율이 높다. 광종별
   기본 basis 매핑을 두는 방안 등 후속 검토 필요.
3. **동일 광산 중복 관측치 우선순위** — 이번에 "같은 연도·basis면 더 큰 값 우선"
   규칙을 추가했지만, 애초에 한쪽 문서에서 추출이 실패하면(§6) 비교할 대상 자체가
   없어 이 규칙이 발동하지 못한다 — 근본 해결은 §7-1과 연결.
4. **§0 데이터 현황** — PRD 표(154건)와 현재 실제 모집단(99건)이 다르다. 코발트
   (20→5)·우라늄(40→11)은 차이가 커서, 검증 대상 문서 자체가 PRD 작성 시점보다
   크게 줄었다 — 담당자 확인 필요(중복 제거가 의도대로 된 것인지, 원본 landing이
   비어 있는지).

## 8. 응답 시간 (§5-3·§5-6)

| 케이스 | 문서 수 | 응답시간 |
|---|---|---|
| 동 생산량 최대(mine_aggregate 단독 호출) | 20 | 21~35초 |
| 동 생산량 최대(전체 파이프라인, dense 포함) | 20 | 41~56초 |
| 동 rank(상위5) | 20 | 21~35초 |
| 니켈 최대 | 23 | 8~10초 |
| 리튬 매장량 최대 | 28 | 10~12초 |
| 우라늄 rank5(수치 없음 확정까지) | 11 | 59~60초 |
| 코발트 compare | 5 | 6~7초 |
| 철광석 최대 | 12 | 7초 |
| 회귀(country-agentic) | - | 11.3초 |
| 회귀(xlsx 위치조회) | - | 9.4초 |
| 회귀(가격+표) | - | 7.7초 |
| 회귀(off_topic) | - | 0.4초 |

`LLM_CONCURRENCY=8` 기준 실측 — 문서 20건이면 최소 3라운드, 우라늄 11건은 문서당
추출이 유독 오래 걸려(basis 판정이 애매한 케이스가 많아 응답이 김) 59초까지 늘어남.
PRD §7이 우려한 "40건이면 상한 필요"는 이번 실측 모집단(최대 28건)에서는 아직
문제가 되지 않았다 — 실제 40건 규모가 회복되면(§7-4) 재실측 필요.
